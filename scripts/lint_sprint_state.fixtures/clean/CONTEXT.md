# Fixture Sprint — `clean`

Synthetic sprint workspace used only to prove `scripts/lint_sprint_state.py`
passes on a well-formed `state.md`/`decisions.md` pair. Not a real sprint.

## Goal

Exercise a legal `may_run_with` pairing (`verification`/`security`), a
`not-applicable` stage with complete decision metadata, and a genuinely
`blocked` stage — with zero validator defects.
