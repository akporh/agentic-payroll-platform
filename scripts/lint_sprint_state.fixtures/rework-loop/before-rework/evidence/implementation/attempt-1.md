# Fixture Evidence (synthetic) — `rework-loop`, `implementation`, attempt 1

Not real product evidence. Stands in for a first implementation attempt
that `audit` later found deficient in `after-rework`.
