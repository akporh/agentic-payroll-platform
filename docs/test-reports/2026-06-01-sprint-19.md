# Sprint 19 Test Report — 2026-06-01

## Summary

| Metric | Value |
|---|---|
| Sprint | 19 — Snapshot Engine (Employee Refactor Phase 2) |
| Date | 2026-06-01 |
| Test suite | 270 passed, 1 skipped, 0 failed |
| LIVE checks | 6 |
| STATIC / CODE REVIEW checks | 9 |
| BLOCKED checks | 0 |
| Overall verdict | **PASS** |

---

## Environment

| Check | Result |
|---|---|
| `alembic current` | `b5c6d7e8f9a0 (head)` — Sprint 19 migration applied |
| `alembic heads` | `b5c6d7e8f9a0` — single head, no divergence |
| Backend | Not running (all verifications via TestClient / direct DB) |
| Frontend TypeScript | `npx tsc --noEmit` — clean, 0 errors |

---

## Migration Integrity

| Check | Result | Type |
|---|---|---|
| Sprint 19 migration `b5c6d7e8f9a0` applied | HEAD confirmed | STATIC |
| `employee_contract_snapshot` table exists | EXISTS | LIVE |
| `component_metadata_snapshot` table exists | EXISTS | LIVE |
| `client_component_metadata_snapshot` table exists | EXISTS | LIVE |
| `employee_contract_snapshot.salary_definition_id` NOT NULL | `is_nullable = NO` | LIVE |
| `payroll_result.salary_inputs_snapshot` NOT NULL | `is_nullable = NO` | LIVE |
| `trg_run_snapshot_immutable` trigger present | EXISTS | LIVE |
| FK constraints on all three snapshot tables | All present (confirmed via information_schema) | LIVE |

---

## Sprint Items Verified

### T1 — Migration (`b5c6d7e8f9a0_sprint19_snapshot_tables.py`)

| Check | Result | Type |
|---|---|---|
| All 3 snapshot tables exist in DB | PASS | LIVE |
| NOT NULL on `salary_definition_id` | PASS — `is_nullable = NO` | LIVE |
| NOT NULL on `salary_inputs_snapshot` | PASS — `is_nullable = NO` | LIVE |
| Immutability trigger present | PASS | LIVE |
| FK constraints enforced | PASS | LIVE |

### T2 — SnapshotService (`snapshot_service.py`)

| Check | Result | Type |
|---|---|---|
| `validate_snapshot_complete` raises `ValueError` for absent run | PASS — raised as expected | LIVE |
| Single UNION ALL query replaces two COUNT queries | PASS — code confirmed | STATIC PASS |
| Imports at top of file, helpers before public API | PASS | STATIC PASS |
| `_col()` dual-type helper removed | PASS — deleted | STATIC PASS |
| Docstring clarifies `client_component_metadata_snapshot` excluded intentionally | PASS | CODE REVIEW |

### T3–T6 — Repo / Persister / Retry / Route

| Check | Result | Type |
|---|---|---|
| `save_payroll_run()` deleted — no callers | PASS — 270 tests pass without it | STATIC PASS |
| `rules_context_snapshot` param removed from `finalise_payroll_run` | PASS — call site updated | STATIC PASS |
| `_sanitize` in retry service replaced with `sanitize_jsonb` from `json_utils` | PASS — import chain clean | STATIC PASS |
| `json_utils.sanitize_jsonb` serialises Decimal as float (not string) | PASS — verified programmatically | LIVE |
| PAYE folded into single totals query (4th column) | PASS — code confirmed, test asserts semantics | STATIC PASS |

### T7 — Frontend (`payroll.ts`, `PayrollResults.tsx`)

| Check | Result | Type |
|---|---|---|
| TypeScript compiles without errors | PASS — `tsc --noEmit` clean | STATIC PASS |

---

## Security Fixes Verified

| Check | Result | Type |
|---|---|---|
| `run_type` allowlist rejects `INVALID` → 422 | PASS | LIVE |
| `retry_strategy` allowlist rejects `FULL_RUN` → 422 | PASS | LIVE |
| Valid `run_type`/`retry_strategy` defaults pass → 404 at workspace lookup | PASS | LIVE |
| `payroll_result` bulk query includes workspace JOIN | PASS — cross-workspace attempt → 404 | LIVE |

---

## Arch-Council Conditions Verified

| Condition | Resolution | Verified |
|---|---|---|
| P1a — `_sanitize_json` layering violation | `backend/infra/json_utils.py` created; `float` semantics canonical | STATIC PASS |
| P1b — `validate_snapshot_complete` docstring gap | Docstring clarified: 3rd table excluded intentionally | CODE REVIEW |
| P2 — Stuck-DRAFT recovery | DRAFT cancellation endpoint scoped to backlog | DEFERRED |
| P3 — Retry totals test (semantic assertions) | Step 9b added to `test_payroll_retry.py` | PASS (in suite) |

---

## Data Integrity Spot-Check

| Check | Result | Notes |
|---|---|---|
| Null `net_pay` on SUCCESS rows | 0 rows — OK | |
| Missing `component_trace_jsonb` on SUCCESS rows | 20 rows | **Pre-existing** — runs from Feb 2026 before sequential executor was mandatory; legacy executor path. Not a Sprint 19 regression. |
| Duplicate statutory rules | 0 — OK | |
| Multiple active pay cycles | 0 — OK | |
| Orphaned DRAFT runs (no snapshot) | 1 row | **Pre-existing** — single run created 2026-02-17, predates snapshot engine. No snapshot tables existed then. D6 guard will block any retry attempt on it. |
| Snapshot rows without parent `payroll_run` | 225 rows in `employee_contract_snapshot`, 3834 in `component_metadata_snapshot` | **Test artifacts** — FK constraints confirmed present; these rows exist only because test teardown uses `session_replication_role = replica` to bypass FK. No live run is orphaned. `client_component_metadata_snapshot` has 0 orphans (correct — no overrides configured in test workspace). |

---

## Regression Suite

All 270 tests passed. Targeted Sprint 19 suite:

```
tests/test_payroll_snapshot_integrity.py::test_snapshot_frozen_after_salary_change        PASSED
tests/test_payroll_snapshot_integrity.py::test_calculations_snapshot_json_immutable_to_direct_update  PASSED
tests/test_payroll_snapshot_integrity.py::test_gross_components_frozen_after_salary_change PASSED
tests/test_payroll_snapshot_integrity.py::test_no_new_calculated_audit_after_reexecution_attempt      PASSED
tests/test_payroll_retry.py::test_payroll_retry_e2e                                       PASSED
5 passed in 1.43s
```

---

## Known Test Failures

No new failures introduced. All pre-existing failures (TF-1 through TF-6) remain resolved from Sprint 18.

ROADMAP Known Test Failures table — no updates required.

---

## Deferred

| Item | Reason |
|---|---|
| DRAFT cancellation endpoint (P2) | New scope — confirmed by Michael as a dedicated cancellation API endpoint. Scoped for Sprint 20/21. |
| Lifecycle endpoints missing workspace scoping (`/retry`, `/approve`, `/lock`, `/pay`) | Pre-existing, not Sprint 19. Scoped as Sprint 20 hardening. |
| Backend LIVE E2E via running server | Backend was not running this session. All API verifications performed via FastAPI TestClient (in-process, connects to live DB). TestClient verifications are functionally equivalent for route-level checks. |

---

## Sign-off

Verified by: Claude Code (automated)
Sprint 19 verdict: **PASS — ready to commit**
