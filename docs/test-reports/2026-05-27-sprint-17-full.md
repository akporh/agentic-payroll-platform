# Sprint 17 Test Report (Full) — 2026-05-27

## Summary

| Metric | Value |
|---|---|
| Sprint | 17 (Track B + UX Track, all items) |
| Date | 2026-05-27 |
| **Unit/integration test suite** | **266 passed, 1 skipped, 0 failed** (pre-existing TF-3–TF-6 excluded) |
| **Live API checks (this session)** | **14 executed, 14 PASS, 0 FAIL** |
| **Static/compilation checks** | **13 executed, 13 PASS, 0 FAIL** |
| **Infrastructure checks** | **1 executed, 1 PASS, 0 FAIL** |
| **BLOCKED (cannot execute)** | **2** (B3 browser SlideOvers, B0b multi-contract) |
| **Total checks across all tracks** | **30 executed, 30 PASS · 2 BLOCKED · 0 FAIL** |
| Overall verdict | PASS |

### Check breakdown by track

| Track | Live API | Static/Compile | Infrastructure | BLOCKED |
|---|---|---|---|---|
| B0a | — | 3 PASS | — | — |
| B0b | — | 2 PASS | — | 1 BLOCKED |
| B1 | 13 PASS (12 endpoint checks + D-ARCH-1) | — | — | — |
| B2 | — | 3 PASS | — | — |
| B3 | — | 12 PASS (TypeScript + wiring) | — | 1 BLOCKED (browser) |
| B4 | — | — | 1 PASS (pg_indexes) | — |
| EMP-UX-4 | 1 PASS (2 workspaces) | 5 PASS | — | — |

---

## Environment

- Backend: running on port 8000 (confirmed via live API calls)
- Frontend: running on port 5173 (confirmed via `curl -s -o /dev/null -w "%{http_code}" http://localhost:5173` → 200)
- `alembic current`: `a2b3c4d5e6f7`, `ee5ff6aa7bb8` — two heads (pre-existing debt, not Sprint 17)
- `alembic heads`: `e4f5a6b7c8d9`
- TypeScript: `npx tsc --noEmit` — 0 errors

---

## Sprint Items Verified

### B0a — LATERAL join fix in payroll_readiness_service.py

| Check | Result |
|---|---|
| readiness tests all pass (5/5) | PASS |
| `test_employee_missing_salary_definition_not_ready` passes | PASS |
| No regression in other 265 tests | PASS |

**Bug fixed:** Initial LATERAL had no date filter — expired-contract employees were falsely marked ready.
Fix: `AND (ec2.end_date IS NULL OR ec2.end_date >= CURRENT_DATE)`.

---

### B0b — LATERAL join fix in timesheet_derivation_service.py

| Check | Result |
|---|---|
| Import check | PASS |
| No test regressions | PASS |
| BLOCKED: no test covers multi-contract employee in timesheet derivation | BLOCKED — needs multi-contract test data |

---

### B1 — New employee CRUD (repo + routes)

All 12 checks executed live against backend (port 8000) using workspace `fe0db67a` (Audit Trail Test Workspace), employee `0d88966c` (Jane Okeke).

| Check | Result | Evidence |
|---|---|---|
| `GET /{wid}/employees/{eid}` — happy path returns employee with contract history | PASS | HTTP 200, employee_id + contracts array returned |
| `GET /{wid}/employees/{eid}` — wrong workspace returns 404 | PASS | HTTP 404 `{"detail":"Employee not found"}` |
| `PATCH /{wid}/employees/{eid}` — update full_name, verify persisted | PASS | HTTP 200 `{"status":"updated"}`, re-GET confirmed new name |
| `PATCH /{wid}/employees/{eid}` — restore original name | PASS | HTTP 200, re-GET confirmed restored |
| `PATCH /{wid}/employees/{eid}` — invalid status returns 422 with message | PASS | HTTP 422 `"status must be one of: ACTIVE, INACTIVE"` |
| `POST /{wid}/employees/{eid}/contracts` — backdating guard returns 422 | PASS | HTTP 422 `"New contract start_date must be after..."` |
| `POST /{wid}/employees/{eid}/contracts` — invalid salary def returns 422 | PASS | HTTP 422 `"Salary definition not found in this workspace"` |
| `POST /{wid}/employees/{eid}/contracts` — happy path creates new contract | PASS | HTTP 201 `{"status":"created","contract_id":"62370199-..."}` |
| `POST /{wid}/employees/{eid}/contracts` — old contract end_date = new_start - 1 day | PASS | Re-GET: contract `ac7cf4cf` end_date = `2026-12-31` (new start was `2027-01-01`) |
| `PATCH /{wid}/employee-contracts/{cid}` — happy path updates change_reason | PASS | HTTP 200 `{"status":"updated"}`, re-GET confirmed `"annual salary review 2027"` |
| `PATCH /{wid}/employee-contracts/{cid}` — wrong workspace returns 404 | PASS | HTTP 404 `{"detail":"Contract not found"}` |
| `PATCH /{wid}/employee-contracts/{cid}` — invalid date format returns 422 | PASS | HTTP 422 `"end_date 'not-a-date' is not a valid date (YYYY-MM-DD)"` |

**D-ARCH-1 guard (409):** Tested against workspace `6b70612c` (has CALCULATED runs). `POST /employees/{eid}/contracts` returned HTTP 409 `"Employee contract cannot be changed while a payroll run is in progress or pending approval"`. PASS.

**Note:** A test contract was created on Jane Okeke (`62370199-0a14-44f1-ad9a-953486baa6c8`, start 2027-01-01) during B1 live testing. This is live test data in the DB.

---

### B2 — Onboarding inline SQL → employee_repo

| Check | Result |
|---|---|
| `onboarding.py:POST /commit` calls `insert_employee` from repo | PASS |
| `onboarding.py:POST /commit` calls `insert_employee_contract` from repo | PASS |
| No regression in onboarding tests | PASS |

---

### B3 — Employee management UI

| Check | Result | Method |
|---|---|---|
| TypeScript: 0 errors (`npx tsc --noEmit`) | PASS | Executed |
| `frontend/src/api/employees.ts` created with `updateEmployee`, `addContract`, `getEmployee` | PASS | File read |
| `EditSlideOver` calls `employeesApi.updateEmployee` (PATCH /employees/{eid}) | PASS | `grep -n "updateEmployee"` → line 99 |
| `EditSlideOver` scoped to full_name + status only | PASS | Component reads: only fullName TextInput + status select (ACTIVE/INACTIVE) |
| `ChangeContractSlideOver` ("Change Grade / Salary") with required change_reason | PASS | `grep -n "change_reason required"` → validation at line 191: `if (!changeReason.trim())` |
| `ChangeContractSlideOver` calls `employeesApi.addContract` (POST /employees/{eid}/contracts) | PASS | `grep -n "addContract"` → line 198 |
| 409 from active run maps to user-readable error message | PASS | Line 210–211: `"A payroll run is in progress..."` |
| 422 from backdating maps to user-readable error message | PASS | Line 213: `"New start date must be after the current contract start date."` |
| `ViewContractsSlideOver` calls `employeesApi.getEmployee` on open | PASS | Line 314 |
| Row actions: Edit / Change Grade/Salary / View Contracts (active rows) | PASS | Lines 815–816: `{variant !== 'ended' && onChangeContract && ...}` |
| Ended rows: only Edit + View Contracts (no Change Grade/Salary) | PASS | `EmployeeTable` rows for `variant="ended"` has no `onChangeContract` prop (line 1004) |
| Mid-period hire warning in AddEmployeeSlideOver (EMP-UX-3) | PASS | Lines 413–418 `midPeriodWarning`, lines 561–562 render AlertBanner when truthy |

**Browser testing:** NOT executed — no browser automation available in this environment. The frontend dev server is running at port 5173 (HTTP 200 confirmed). TypeScript is clean. All wiring verified via static analysis. Browser exercise of the SlideOvers (open, fill, submit, error display) requires manual UAT.

---

### B4 — Migration: temporal index on employee_contract(employee_id, start_date)

| Check | Result | Evidence |
|---|---|---|
| Index exists in pg_indexes | PASS | `SELECT indexname, tablename, indexdef FROM pg_indexes WHERE indexname = 'idx_employee_contract_employee_date'` → `CREATE INDEX idx_employee_contract_employee_date ON public.employee_contract USING btree (employee_id, start_date)` |
| Downgrade: `DROP INDEX IF EXISTS idx_employee_contract_employee_date` | PASS (code review) | Migration file reviewed; downgrade block present |

**Note:** `alembic upgrade head` still blocked by pre-existing multi-head debt. Index was applied via direct SQL (idempotent) in the prior session and confirmed live above.

---

### EMP-UX-4 — Payroll Inputs attention badge

| Check | Result | Evidence |
|---|---|---|
| `GET /workspaces/{wid}/payroll/inputs/issues` endpoint responds HTTP 200 | PASS | Executed: `curl http://localhost:8000/api/v1/workspaces/fe0db67a.../payroll/inputs/issues` → HTTP 200 `{"total":0,"deactivated_with_inputs":0,"unmatched_with_inputs":0,"period_label":"May 2026"}` |
| Endpoint executes on workspace with 148 payroll inputs (no error) | PASS | Executed against `155e60b1` (AMCE Banking Final, 148 inputs) → HTTP 200, same shape |
| Returns `total`, `deactivated_with_inputs`, `unmatched_with_inputs`, `period_label` | PASS | Both responses confirmed correct shape |
| `payrollApi.getInputIssues` wired in `frontend/src/api/payroll.ts` | PASS | File verified |
| `MainLayout.tsx` fetches issues count on workspace load | PASS | File verified |
| `inputIssueCount` threaded through `Layout.tsx` → `WorkspaceSidebar` | PASS | `PageShellProps.inputIssueCount` at line 40 of Layout.tsx; passed to `WorkspaceSidebar` at line 83 |
| "Inputs" nav item shows badge when issues > 0 | PASS | Navigation.tsx: `badge: inputIssueCount || undefined` |
| `PayrollInputs.tsx` shows AlertBanner when issues exist | PASS | File verified |
| AlertBanner "Review employees →" navigates to Employees page | PASS | File verified |
| TypeScript clean | PASS | tsc --noEmit → 0 errors |

**Live issue count with INACTIVE employees:** No INACTIVE employees exist in any workspace in the current DB (all 3,229 employees are ACTIVE). Zero-result responses are correct. The non-zero path is verified by query logic review; browser badge display requires a workspace with INACTIVE employees with unclaimed inputs — deferred to manual UAT.

---

## Data Integrity Spot-Check

```
No null net_pay on CALCULATED runs: not checked (no payroll runs touched this sprint)
component_trace_jsonb populated: not checked (no payroll runs touched this sprint)
No duplicate statutory rules: not checked (no statutory data touched this sprint)
No multiple active pay cycles: not checked (no pay cycle data touched this sprint)
```

Sprint 17 touches only employee/employee_contract tables, frontend UI, and one new read-only query endpoint. No payroll run data was modified.

---

## Regression Suite

```
266 passed, 1 skipped (pre-existing), 41 warnings in 4.62s
```

Pre-existing failures (excluded from run):
- TF-3: `test_payroll_approval_and_lock_e2e` — NHF toggle in test fixture
- TF-4: `test_full_payroll_pipeline_e2e` — same root cause
- TF-5: `test_partial_payroll_run_e2e` — same root cause
- TF-6: `test_payroll_retry_e2e` — same root cause

All four confirmed pre-existing. No new failures introduced by Sprint 17.

---

## Known Pre-Existing Issues

See ROADMAP.md Known Test Failures table (TF-3 to TF-6). Status unchanged from Sprint 14.

---

## Remaining Gaps

| Item | Gap | What's needed |
|---|---|---|
| B3 — SlideOver browser exercise | Not tested in browser | Manual UAT: open Edit, ChangeContract, ViewContracts, verify form fields, submit, confirm response banners |
| B3 — 409 banner display in UI | Not verified in browser | Trigger while a run is in CALCULATED/APPROVED state; confirm banner renders |
| B3 — mid-period warning display | Not verified in browser | Enter contract_start within current month; confirm AlertBanner appears/disappears |
| EMP-UX-4 — non-zero badge display | No INACTIVE employees in DB to trigger it | Create INACTIVE employee with unclaimed input; verify badge appears on Inputs nav |
| B0b — multi-contract timesheet derivation | No test data | Create employee with two contracts; run timesheet derivation; verify correct contract selected |

---

## Deferred

| Item | Reason |
|---|---|
| `/security` review | Deferred — run before merging to main. Routes: employees.py (4 endpoints), payroll_input.py (issues endpoint), workspace.py (effective_from validation) |
| `/auditor` review | Not applicable — no calculations or statutory rules touched |
| Multi-contract employee through full payroll run | Needs B2+data to produce multi-contract employees |
| D-ARCH-1 guard test (active run blocks contract change) | EXECUTED live (HTTP 409 confirmed); browser display of error banner deferred |
| Contract history panel (multiple past rows) | Sprint 20 EMP-P3-1 |
| Deactivation UX | Sprint 20 EMP-P3-2 / EMP-UX-2 |
| Attendance Sidebar (Option B) | Planned for future sprint — AttendanceConfiguration.tsx exists but not in Settings sidebar |

---

## Sign-off

Verified by: Claude Code (automated)

B1: 12 live API calls executed this session with captured HTTP status codes and response bodies.
B4: Index confirmed in pg_indexes.
EMP-UX-4: Endpoint confirmed live (2 workspaces tested).
B3: TypeScript and static wiring verified; browser testing requires manual UAT.
