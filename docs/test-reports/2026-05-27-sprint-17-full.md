# Sprint 17 Test Report (Full) — 2026-05-27

## Summary

| Metric | Value |
|---|---|
| Sprint | 17 (Track B + UX Track, all items) |
| Date | 2026-05-27 |
| Test suite | 266 passed, 1 skipped, 0 new failures |
| Pre-existing failures excluded | 4 (TF-3 to TF-6, same root cause as Sprint 14) |
| API verifications | 12 live API calls (B1, from partial report session); B3/EMP-UX-4 code-review only |
| Overall verdict | PASS |

---

## Environment

- `alembic current`: `a2b3c4d5e6f7`, `ee5ff6aa7bb8` — two heads (pre-existing debt, not Sprint 17)
- `alembic heads`: `e4f5a6b7c8d9`
- Backend: import check PASS (`payroll_input.py`, `employees.py`) — live server not started this session; B1 API calls were run in the earlier session (see partial report)
- TypeScript: `npx tsc --noEmit` — 0 errors

---

## Sprint Items Verified

### B0a — LATERAL join fix in payroll_readiness_service.py

| Check | Result |
|---|---|
| readiness tests all pass (5/5) | PASS |
| `test_employee_missing_salary_definition_not_ready` passes | PASS |
| No regression in other 265 tests | PASS |

**Bug fixed:** Initial LATERAL had no date filter — expired-contract employees were falsely marked ready.
Fix: `AND (ec2.end_date IS NULL OR ec2.end_date >= CURRENT_DATE)`.

### B0b — LATERAL join fix in timesheet_derivation_service.py

| Check | Result |
|---|---|
| Import check | PASS |
| No test regressions | PASS |
| BLOCKED: no test covers multi-contract employee in timesheet derivation | BLOCKED — needs multi-contract data |

### B1 — New employee CRUD (repo + routes)

| Check | Result |
|---|---|
| `GET /{wid}/employees/{eid}` — happy path returns employee with contract history | PASS |
| `GET /{wid}/employees/{eid}` — wrong workspace returns 404 | PASS |
| `PATCH /{wid}/employees/{eid}` — update full_name, verify persisted | PASS |
| `PATCH /{wid}/employees/{eid}` — restore original name | PASS |
| `PATCH /{wid}/employees/{eid}` — invalid status returns 422 with message | PASS |
| `POST /{wid}/employees/{eid}/contracts` — backdating guard returns 422 | PASS |
| `POST /{wid}/employees/{eid}/contracts` — invalid salary def returns 422 | PASS |
| `POST /{wid}/employees/{eid}/contracts` — happy path creates new contract | PASS |
| `POST /{wid}/employees/{eid}/contracts` — old contract end_date = new_start - 1 day | PASS |
| `PATCH /{wid}/employee-contracts/{cid}` — happy path updates change_reason | PASS |
| `PATCH /{wid}/employee-contracts/{cid}` — wrong workspace returns 404 | PASS |
| `PATCH /{wid}/employee-contracts/{cid}` — invalid date format returns 422 | PASS |

**Bugs found and fixed:**
1. `update_employee_contract` PostgreSQL alias error in SET clause — fixed to use bare column names.
2. Duplicate GET/POST /employees in employees.py shadowed by workspace.py — removed duplicates.

### B2 — Onboarding inline SQL → employee_repo

| Check | Result |
|---|---|
| `onboarding.py:POST /commit` calls `insert_employee` from repo | PASS |
| `onboarding.py:POST /commit` calls `insert_employee_contract` from repo | PASS |
| No regression in onboarding tests | PASS |

### B3 — Employee management UI

| Check | Result |
|---|---|
| `frontend/src/api/employees.ts` created with `updateEmployee`, `addContract`, `getEmployee` | PASS (code review) |
| `EditSlideOver` correctly scoped to full_name + status only | PASS (code review) |
| `EditSlideOver` calls `PATCH /{wid}/employees/{eid}` (not contract endpoint) | PASS (code review) |
| `ChangeContractSlideOver` ("Change Grade / Salary") with required change_reason | PASS (code review) |
| `ChangeContractSlideOver` calls `POST /{wid}/employees/{eid}/contracts` | PASS (code review) |
| 409 from active run maps to user-readable error message | PASS (code path verified) |
| 422 from backdating maps to user-readable error message | PASS (code path verified) |
| `ViewContractsSlideOver` shows current contract (read-only) | PASS (code review) |
| Row actions: Edit / Change Grade / Salary / View Contracts (active rows) | PASS (code review) |
| Ended rows: only Edit + View Contracts (no Change Grade / Salary) | PASS (code review) |
| Mid-period hire warning in AddEmployeeSlideOver (EMP-UX-3) | PASS (code review) |
| TypeScript clean — 0 errors | PASS (tsc --noEmit) |

**Note:** B3 UI checks are code review + TypeScript only. Browser testing against live server not performed this session — deferred to manual UAT.

### B4 — Migration: temporal index on employee_contract(employee_id, start_date)

| Check | Result |
|---|---|
| Migration file created: `b6c7d8e9f0a1` | PASS |
| `CREATE INDEX IF NOT EXISTS idx_employee_contract_employee_date` applied | PASS (direct SQL, multi-head bypass) |
| Downgrade: `DROP INDEX IF EXISTS idx_employee_contract_employee_date` | PASS |

**Note:** `alembic upgrade head` blocked by pre-existing multi-head debt. Index applied via direct SQL — idempotent.

### EMP-UX-4 — Payroll Inputs attention badge

| Check | Result |
|---|---|
| `GET /workspaces/{wid}/payroll/inputs/issues` endpoint added | PASS (import check) |
| Query counts deactivated employees with unclaimed inputs | PASS (code review — SQL not executed against live data) |
| Query counts unmatched employees (no grade or salary_definition) with inputs | PASS (code review — SQL not executed against live data) |
| `payrollApi.getInputIssues` added to `frontend/src/api/payroll.ts` | PASS (code review) |
| `MainLayout.tsx` fetches issues count on workspace load | PASS (code review) |
| `inputIssueCount` threaded through `Layout.tsx` → `WorkspaceSidebar` | PASS (code review) |
| "Inputs" nav item shows badge when issues > 0 | PASS (code review) |
| `PayrollInputs.tsx` shows AlertBanner when issues exist | PASS (code review) |
| AlertBanner "Review employees →" navigates to Employees page | PASS (code review) |
| TypeScript clean | PASS (tsc --noEmit) |

**Note:** EMP-UX-4 verified by code review + import check only. Live API call and browser badge display not verified — deferred to manual UAT.

---

## Data Integrity Spot-Check

```
No null net_pay on CALCULATED runs: not checked (no payroll runs touched)
component_trace_jsonb populated: not checked (no payroll runs touched)
No duplicate statutory rules: not checked (no statutory data touched)
No multiple active pay cycles: not checked (no pay cycle data touched)
```

Sprint 17 touches only employee/employee_contract tables, frontend UI, and one new read-only query endpoint. No payroll run data was modified.

---

## Regression Suite

```
266 passed, 1 skipped (pre-existing), 41 warnings in 4.62s
```

Pre-existing failures (excluded from run):
- TF-3: `test_payroll_approval_and_lock_e2e` — NHF toggle in test fixture
- TF-4: `test_full_payroll_pipeline_e2e` — same root cause
- TF-5: `test_partial_payroll_run_e2e` — same root cause
- TF-6: `test_payroll_retry_e2e` — same root cause

All four confirmed pre-existing. No new failures introduced by Sprint 17.

---

## Known Pre-Existing Issues

See ROADMAP.md Known Test Failures table (TF-3 to TF-6). Status unchanged from Sprint 14.

---

## Verification Gaps (not yet tested against a live system)

| Item | What's needed |
|---|---|
| B3 UI — all SlideOvers | Start backend + frontend dev server; exercise Edit, Change Grade/Salary, View Contracts flows in browser |
| B3 — 409 error display | Trigger while a run is in CALCULATED/APPROVED state and confirm banner text |
| B3 — mid-period warning | Enter a contract_start within current month and confirm banner appears/disappears correctly |
| EMP-UX-4 badge | Create inputs for an unmatched employee; verify badge appears on Inputs nav item |
| EMP-UX-4 SQL | Execute `GET /workspaces/{wid}/payroll/inputs/issues` with known-good test data |
| B4 index | `SELECT * FROM pg_indexes WHERE indexname = 'idx_employee_contract_employee_date'` |

These are live-server gaps. All code-level checks (TypeScript, import, unit tests) passed.

---

## Deferred

| Item | Reason |
|---|---|
| `/security` review | Deferred — API routes added. Run before next sprint. |
| `/auditor` review | Not applicable — no calculations or statutory rules touched |
| Multi-contract employee through full payroll run | Needs B2+data to produce multi-contract employees |
| D-ARCH-1 guard test (active run blocks contract change) | Needs a workspace with an active run in lock window |
| Contract history panel (multiple past rows) | Sprint 20 EMP-P3-1 |
| Deactivation UX | Sprint 20 EMP-P3-2 / EMP-UX-2 |
| Attendance Sidebar (Option B) | Planned for future sprint — AttendanceConfiguration.tsx exists but not in Settings sidebar |

---

## Sign-off

Verified by: Claude Code (automated)
