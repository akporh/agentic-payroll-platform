# Sprint 16 Test Report — 2026-05-13

## Summary
| Metric | Value |
|---|---|
| Sprint | 16 |
| Date | 2026-05-13 |
| Sprint items | TM-1 through TM-7, C1, C2 |
| Code-level verifications | 22 checks — 22 passed, 0 failed |
| Runtime / live-DB verifications | BLOCKED — see Deferred |
| Overall verdict | PASS (code-level); runtime deferred to staging |

---

## Environment

- `alembic current` output: `a2b3c4d5e6f7 (head)`, `ee5ff6aa7bb8 (head)` (two heads — MIG-C is a standalone AUTOCOMMIT migration, expected)
- Known warning: `Revision a1b2c3d4e5f6 is present more than once` — pre-existing debt from before Sprint 16; not a regression
- Backend runtime verification: deferred (user confirmed prior session ran environment checks)

---

## Test Session Notes

Prior tester session (same branch, same day) ran the full environment check, migration integrity checks, and core API verifications. Context was cleared mid-check on C1. This session verified the remaining delta:
- **C1** (`expected_hours` per-employee): confirmed in `batch_processor.py:89-93`
- **C2** (readiness gate): confirmed in `payroll_readiness_service.py:116-126`
- All remaining code-level items verified via static inspection

---

## Sprint Items Verified

### C1 — Per-employee `expected_hours` from shift_type

| # | Check | Result |
|---|---|---|
| C1-1 | `batch_processor.py:90` — `_hours_per_day_map = {"DAY": 8, "S.DAY": 8, "2_SHIFT": 8, "4_SHIFT": 12}` | PASS |
| C1-2 | `expected_hours` resolved inside per-employee loop (not batch scalar) | PASS |
| C1-3 | `emp_context["expected_hours"]` set before `execute_single_employee_payroll` call | PASS |
| C1-4 | `sequential_executor.py:174` — `full_context = dict(context or {})` propagates `expected_hours` into executor | PASS |
| C1-5 | Fallback: `shift_type or "DAY"` → defaults to 8h/day for NULL shift_type | PASS |

**Notes:** The fallback to DAY (8h) for NULL shift_type is correct since NULL shift_type triggers a hard FAILED at upload/derivation time (TM-3-AC-12). The fallback in batch_processor is a safety net for non-timesheet runs.

---

### C2 — Timesheet completeness gate

| # | Check | Result |
|---|---|---|
| C2-1 | `payroll_readiness_service.py:118` — gate is conditional on `ws_config.get("timesheet_enabled")` | PASS |
| C2-2 | `timesheet_repo.get_non_approved_employees()` called with `(workspace_id, period_start)` | PASS |
| C2-3 | Error message lists employee names with count + "and N more" truncation at 5 | PASS |
| C2-4 | Non-timesheet workspaces are unaffected (gate is behind `timesheet_enabled` check) | PASS |

---

### TM-1 — Workspace timesheet configuration

| # | Check | Result |
|---|---|---|
| TM-1-AC-2 | `workspace_config_repo.py:21` — `"timesheet_enabled": False` in `_DEFAULTS` | PASS |
| TM-1-AC-3 | `_require_timesheet_enabled()` at `payroll.py:1485-1488` returns HTTP 400 when disabled | PASS |
| TM-1-AC-5 | C2 gate in `payroll_readiness_service.py:112-126` blocks `link_inputs_to_run` | PASS |
| TM-1-AC-6 | Template seeding wired in `workspace.py` PUT payroll-config; `ON CONFLICT DO NOTHING` | PASS (code-level) |
| TM-1-AC-7 | Template version warning in `WorkspaceConfig.tsx` | PASS (code-level) |

---

### TM-2 — Timesheet upload

| # | Check | Result |
|---|---|---|
| TM-2: route | `POST /workspaces/{wid}/timesheet/upload` wired; calls `timesheet_derivation_service.upload_timesheet` | PASS |
| TM-2-AC-3 | Unknown code rejection delegated to service layer | PASS (code-level) |
| TM-2-AC-6 | `payroll_input_repo.delete_unclaimed_timesheet_inputs()` uses `WHERE source = 'TIMESHEET' AND payroll_run_id IS NULL` | PASS |
| TM-2-AC-8 | Upload sets `derivation_status = 'PENDING'` | PASS (code-level) |

---

### TM-3 — Timesheet derivation pipeline

| # | Check | Result |
|---|---|---|
| TM-3-AC-1 | `timesheet_derivation.py` imports: stdlib only (`enum`, `dataclasses`, `datetime`, `decimal`, `collections`, `typing`) — zero infra imports | PASS |
| TM-3-AC-5 | Three-step cap formula at `timesheet_derivation.py:244-256` matches spec exactly | PASS |
| TM-3-AC-7 | `resolve_hours()` at `timesheet_derivation.py:63-87` — all 5 dispatch cases correct; quantize to 2dp | PASS |
| TM-3-AC-8 | `classify_day()` at `timesheet_derivation.py:34-45` — precedence: PH > SAT (DAY-only) > WEEKDAY | PASS |
| TM-3-AC-12 | `shift_type IS NULL` → FAILED status; no silent fallback to 8h | PASS (code-level) |
| TM-3-AC-16 | `PENDING → DERIVED` (success) / `PENDING → FAILED` (error) — `APPROVED` only set by approval step | PASS (code-level) |
| TM-3-AC-18 | Client B three-employee worked example | BLOCKED — requires live staging run against test xlsx |

---

### TM-4 — Manual OT override

| # | Check | Result |
|---|---|---|
| TM-4-AC-4 | Re-derivation scoped delete: `WHERE source = 'TIMESHEET' AND payroll_run_id IS NULL` does not touch `MANUAL_OT` rows | PASS |

---

### TM-5 — Timesheet-to-pay-instruction flow

| # | Check | Result |
|---|---|---|
| TM-5-AC-3 | `load_inputs_for_run()` at `payroll_input_repo.py:95` — `source` column in SELECT | PASS |
| TM-5-AC-3 | Retry path at `payroll_retry_service.py:504` — uses same `load_inputs_for_run()` | PASS |
| TM-5-AC-3 | Retry path at `payroll_retry_service.py:806` — uses same `load_inputs_for_run()` | PASS |
| TM-5-AC-4 | `executor.py:259-260` — `if full_context.get("timesheet_source"): _hire_proration_applied = False` | PASS |
| TM-5-AC-4 | `timesheet_source` set in `batch_processor.py:104` and propagated via `emp_context` | PASS |
| TM-5-AC-5 | `expected_hours` resolved per-employee in `batch_processor.py:89-93` — not a batch scalar | PASS |
| TM-5-AC-6 | MIG-C updates `uq_payroll_input_unclaimed` to include `source` | PASS (migration code) |
| TM-5-AC-7 | C2 gate blocks `link_inputs_to_run` with non-approved employee list | PASS |

---

### TM-6 — Timesheet audit trail

| # | Check | Result |
|---|---|---|
| TM-6: routes | `GET /workspaces/{wid}/timesheet/status` and `GET /workspaces/{wid}/timesheet/audit/{employee_id}` wired | PASS |
| TM-6-AC-2 | `source` column propagated through load paths — TIMESHEET vs MANUAL_OT distinguishable in audit | PASS |

---

### TM-7 — Attendance code and policy configuration

| # | Check | Result |
|---|---|---|
| TM-7: routes | GET/POST attendance codes, PATCH code, PATCH policy wired in `workspace.py` | PASS (code-level) |
| TM-7-AC-4 | `category` immutability guard present in workspace route | PASS (code-level) |
| TM-7-AC-9 | `counts_as_paid = FALSE AND counts_towards_ot_threshold = TRUE` → HTTP 400 | PASS (code-level) |
| TM-7-AC-10 | JWT workspace ownership check in workspace route | PASS (code-level) |
| TM-7-AC-11 | v1 template seed values in migration match spec table | PASS |

---

## Data Integrity Spot-Check

Not re-run this session (completed in prior session). Pre-existing alembic warning (`a1b2c3d4e5f6` duplicate) is documented migration debt predating Sprint 16.

---

## Regression Suite

Not re-run this session (completed in prior session).

---

## Known Pre-Existing Issues

| Issue | Status |
|---|---|
| Duplicate revision `a1b2c3d4e5f6` in alembic history | Pre-existing — tracked in migration debt memory; not a Sprint 16 regression |
| `workspace_payroll_config.py` imports `TIMESTAMPTZ` from SQLAlchemy 2.0 dialect (does not exist) — works at runtime, breaks standalone import | Pre-existing — noted in handoff; Sprint 16 new models use `DateTime(timezone=True)` |
| `Toggle` component `hint` prop availability in design system | Unverified — needs visual QA in staging |

---

## Deferred

| Item | Reason |
|---|---|
| TM-3-AC-18 — Client B 3-employee worked example | Requires live staging DB + running derivation service against `docs/data/Client B/test sample timesheet-with result.xlsx` |
| MIG-C standalone AUTOCOMMIT verification | `CREATE INDEX CONCURRENTLY` cannot be tested without live DB under concurrent load |
| TM-1-AC-6 — Template seeding end-to-end | Needs live workspace enable → DB inspection of seeded rows |
| Full UI functional testing (TimesheetUpload, AttendanceConfiguration, WorkspaceConfig toggle) | Needs running frontend + backend |
| TM-4-AC-3 — MANUAL OT locked after APPROVED | Needs live run in APPROVED state |

---

## Sign-off

Verified by: Claude Code (automated) — code-level inspection  
Runtime deferred: Client B worked example + MIG-C + UI — **flag for staging verification before Client B onboarding**
