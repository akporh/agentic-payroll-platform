# Sprint 12 M1+M2 Test Report — 2026-05-03

## Summary

| Metric | Value |
|---|---|
| Sprint | 12 (Track M — M1, M2) |
| Date | 2026-05-03 |
| M1/M2 test suite | 18 passed, 0 failed |
| Full regression suite | 244 passed, 4 failed (pre-existing), 1 skipped |
| Migration syntax | PASS |
| Migration chain | PASS |
| Migration DB application | BLOCKED — DB has multi-head state (see notes) |
| Overall verdict | PASS (domain logic); BLOCKED (migration apply) |

---

## Environment

- Alembic current: `a2b3c4d5e6f7` (head)
- Alembic heads: `a1b2c3d4e5f6`, `a2b3c4d5e6f7`, `c0d1e2f3a4b5`, `d6e7f8a9b0c1`, `e6f7a8b9c0d1`
- DB: `payroll_dev` (local PostgreSQL)
- Backend: not checked (domain-only tests; no API surface changed this sprint)

---

## Sprint Items Verified

### M1 — Non-Taxable Component Class

| Check | ID | Result | Notes |
|---|---|---|---|
| `non_taxable` excluded from `GROSS_PAY` | M1-T1 | **PASS** | GROSS_PAY=500k with earning only; non_taxable MEAL_ALLOWANCE(60k) excluded |
| `non_taxable` included in `NET_PAY` | M1-T2 | **PASS** | NET_PAY = baseline + 60k; employee receives allowance |
| `TAXABLE_INCOME` unaffected by non_taxable | M1-T3 | **PASS** | TAXABLE_INCOME identical to baseline run |
| Existing earning components unaffected | M1-T4 | **PASS** | GROSS_PAY baseline unchanged |
| `component_trace_jsonb` records class correctly | M1-T5 | **PASS** | MEAL_ALLOWANCE trace entry: component_class='non_taxable' |
| D-M1-4: workspace override via `client_meta` | M1-T6 | **PASS** | TRANSPORT promoted to non_taxable: GROSS_PAY=450k, NET_PAY=394k (>386.5k baseline — PAYE saving), TAXABLE_INCOME<baseline |
| D-INV-1: `is_pensionable=True` forbidden on `non_taxable` | M1-T7 | **PASS** | Documented in CLAUDE.md data contract table; enforced by convention (no DB guard yet — noted in migration comments) |
| Migration `c5d6e7f8a9b0` syntax | M1-T8a | **PASS** | `ast.parse` clean |
| Migration `c5d6e7f8a9b0` chain | M1-T8b | **PASS** | down_revision=f9a0b1c2d3e4 (existing) ✓ |
| Migration pre-check passes on live DB | M1-T8c | **PASS** | All live component_class values in allowed set; no ck_component_metadata_class exists |
| Migration DB apply | M1-T8d | **BLOCKED** | DB has 5 heads (multi-head state); need merge migration or targeted `alembic upgrade c5d6e7f8a9b0` |

### M2 — PAYE-Only Additions Path

| Check | ID | Result | Notes |
|---|---|---|---|
| `PAYE_ONLY_ADDITIONS` added to `TAXABLE_INCOME` | M2-T1 | **PASS** | TAXABLE_INCOME = baseline + 50k (exact match) |
| `PAYE_ONLY_ADDITIONS` NOT in `GROSS_PAY` | M2-T2 | **PASS** | GROSS_PAY=500k unchanged |
| `PAYE_ONLY_ADDITIONS` NOT in `NET_PAY` | M2-T3 | **PASS** | NET_PAY ≤ baseline (more PAYE deducted; 50k not disbursed) |
| Zero-value paye_only input is no-op | M2-T4 | **PASS** | PAYE_ONLY_ADDITIONS=0.00 |
| `PAYE_ONLY_ADDITIONS` in trace | M2-T5 | **PASS** | Trace entry result='50000.00' |
| Handler reads uppercase `PAYE_ONLY` only | M2-T6 | **PASS** | Lowercase 'paye_only' category → PAYE_ONLY_ADDITIONS=0 |
| Legacy inputs without category are unaffected | M2-T7 | **PASS** | Backward compat confirmed |
| Migration `d6e7f8a9b0c1` syntax | M2-T8a | **PASS** | `ast.parse` clean |
| Migration `d6e7f8a9b0c1` chain | M2-T8b | **PASS** | down_revision=c5d6e7f8a9b0 (M1) ✓ |
| Migration pre-check passes on live DB | M2-T8c | **PASS** | Live input_category values: EARNING, DEDUCTION — both in allowed set; no ck_payroll_input_category exists |
| Migration DB apply | M2-T8d | **BLOCKED** | Same multi-head state issue as M1 |

### INT — Combined M1 + M2

| Check | ID | Result | Notes |
|---|---|---|---|
| GROSS_PAY excludes non_taxable | INT-T1a | **PASS** | GROSS_PAY=500k (MEAL_ALLOWANCE excluded) |
| TAXABLE_INCOME = GROSS_PAY + PAYE_ONLY - pension | INT-T1b | **PASS** | Exact decimal match: 500k + 50k - pension |
| NET_PAY includes non_taxable, excludes paye_only | INT-T1c | **PASS** | NET_PAY with paye_only ≤ NET_PAY without; non_taxable adds to net |
| Full regression suite | INT-T2 | **PASS** | 244 passed, 4 pre-existing failures (TF-3–TF-6, NHF key) |

---

## Migration DB Application — Block Detail

`alembic heads` shows 5 heads. The new M1 migration branches from `f9a0b1c2d3e4` (an existing migration), creating a new branch alongside `a2b3c4d5e6f7` (current DB head).

**To apply:** Either:
1. `alembic upgrade d6e7f8a9b0c1` (applies both M1 and M2 specifically) — requires `f9a0b1c2d3e4` to be in the DB's applied chain.
2. Create a merge migration if multiple branches need to converge for a clean single head.

**Risk:** The DB `payroll_input.input_category` column already exists (`VARCHAR(30)`) and `ck_payroll_input_category` constraint does not exist. The M2 migration's ADD COLUMN guard is not needed (no ADD COLUMN) but the CHECK constraint add is idempotent via `DO $$ EXCEPTION WHEN duplicate_object THEN NULL $$`.

---

## Data Integrity Spot-Check

| Check | Result | Notes |
|---|---|---|
| `payroll_result` null net_pay on any run | 0 rows ✓ | |
| `payroll_result` null trace on SUCCESS | 20 rows | **Pre-existing** — legacy executor runs (no sequential_executor path) |
| Duplicate statutory rules | 2 groups | **Pre-existing** — G7 issue (NG + NULL country_code, effective 2000-01-01) |
| Multiple active pay cycles | 0 rows ✓ | |

---

## Test File Written

`tests/test_sprint12_m1_m2.py` — 18 tests, 3 classes:
- `TestBaseline` (2 tests) — confirms no regression to existing earning sweep
- `TestM1NonTaxable` (6 tests) — M1 acceptance criteria with exact numeric assertions
- `TestM2PayeOnly` (7 tests) — M2 acceptance criteria with exact numeric assertions
- `TestIntegrationM1M2` (3 tests) — combined M1+M2 run

**Test correction noted:** Initial M1-T6 assertion (`NET_PAY == baseline`) was wrong. Corrected to `NET_PAY > baseline` — when TRANSPORT is promoted to `non_taxable`, GROSS_PAY drops (450k), TAXABLE_INCOME drops (410k), PAYE drops by ₦7,500, so NET_PAY is **higher** (₦394k vs ₦386.5k baseline). The employee still receives TRANSPORT in full; they additionally pay less PAYE. This is the correct financial behaviour of non_taxable treatment.

---

## Known Pre-Existing Issues (Known Test Failures Table)

| Test | File | Root Cause | Roadmap Item |
|---|---|---|---|
| test_payroll_approval_and_lock_e2e | test_payroll_lock_and_approval.py | NHF key mismatch (employee_rate vs nhf_rate) in e2e fixture | F1 / SR9 |
| test_partial_payroll_run_e2e | test_payroll_partial_run_e2e.py | NHF key mismatch in e2e fixture | F1 / SR9 |
| test_full_payroll_pipeline_e2e | test_payroll_pipeline_e2e.py | NHF key mismatch in e2e fixture | F1 / SR9 |
| test_payroll_retry_e2e | test_payroll_retry.py | NHF key mismatch in e2e fixture | F1 / SR9 |

All 4 failures pre-date this sprint. No new failures introduced.

---

## Deferred

- **M1-T8d / M2-T8d**: Migration DB apply — blocked on multi-head resolution. Not a blocker for domain logic; both migrations are syntactically correct with valid pre-check guards.
- **M3, M4, M5**: Not in scope for this verification.
- **N1**: Not started; arch-council required.
- **N2**: Blocked — awaiting client Model A/B decision.

---

## Sign-off

Verified by: Claude Code (automated) — 2026-05-03
