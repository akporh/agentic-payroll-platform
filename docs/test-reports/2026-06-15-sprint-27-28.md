# Sprint 27 + 28 + WS-ACTIVATE Fix Test Report — 2026-06-15

## Summary
| Metric | Value |
|---|---|
| Sprints | 27 (Smart Native Upload), 28 (Upload Error Visibility), WS-ACTIVATE Fix |
| Date | 2026-06-15 |
| LIVE API checks | 7 (all PASS) |
| STATIC / CODE REVIEW checks | 12 |
| BLOCKED checks | 3 (frontend-only, no Playwright) |
| Data integrity checks | 4 (3 PASS, 1 WARN pre-existing) |
| TypeScript | STATIC PASS (0 errors) |
| Overall verdict | **PASS** |

---

## Environment
- Backend: uvicorn running on port 8000 (restarted during this session to pick up sprint code)
- Frontend: Vite dev server running on port 5173
- DB: PostgreSQL (local), migrations current
- TypeScript: `cd frontend && npx tsc --noEmit` — 0 errors

---

## Sprint 27 — Smart Native Upload (EMP-NATIVE-1, INP-NATIVE-1, PAY-RECON-1, INP-MULTI-1, EMP-REG-5-FIX)

### EMP-NATIVE-1 — Native employee bulk upload via NativeUploadFlow
| Check | Type | Result |
|---|---|---|
| `NativeUploadFlow.tsx` renders file picker, column mapping panel, preview, submit | CODE REVIEW | STATIC PASS |
| `detectHeaderRow` call removes dead 3rd arg — no runtime breakage | STATIC | STATIC PASS |
| `skippedCount` uses `??` coalesce — two sources are mutually exclusive | CODE REVIEW | STATIC PASS |
| `EMP_NO_ALIASES` moved to module level — no per-render recreation | STATIC | STATIC PASS |

### INP-NATIVE-1 — Native period inputs upload
| Check | Type | Result |
|---|---|---|
| `PayrollInputsBulkUpload` uses `detectHeaderRowByScorer` (removed dead 2nd arg) | STATIC | STATIC PASS |
| `invalidCount` computed as `rows.length - validCount` (single pass) | CODE REVIEW | STATIC PASS |
| Month detection uses word-boundary regex (`\bMAY\b`) — no false match on "MAYFLOWER" | CODE REVIEW | STATIC PASS |

### PAY-RECON-1 — Reconciliation upload (NativeUploadFlow path)
| Check | Type | Result |
|---|---|---|
| `PAYROLL_RECON_ALIASES` and `detectHeaderRow` wiring unchanged | STATIC | STATIC PASS |

### INP-MULTI-1 — Multi-period input support (frontend column detection)
| Check | Type | Result |
|---|---|---|
| `parseInputColumnHeader` correctly parses "THE MONTH OF JANUARY 2026 OVERTIME @ N1000.00" | CODE REVIEW | STATIC PASS |
| `scoreRowAsInputHeaders` used by `detectHeaderRowByScorer` in bulk upload | STATIC | STATIC PASS |

### EMP-REG-5-FIX — Enrollment pre-population fix
| Check | Type | Result |
|---|---|---|
| Sprint 26 fix retained (no regression introduced by simplify) | CODE REVIEW | STATIC PASS |

---

## Sprint 28 — Upload Error Visibility (UPLOAD-ERR-1, UPLOAD-SKIP-1)

### UPLOAD-ERR-1 — 3-layer error display (AlertBanner + amber block + red table)
| Check | Type | Result |
|---|---|---|
| Frontend error rendering pattern (AlertBanner + download block + error table) | BLOCKED | Cannot LIVE-verify without Playwright — UI only |
| `extractError` shared utility imported in all 5 refactored pages | STATIC | STATIC PASS |
| `AttendanceConfiguration.tsx` and `TimesheetUpload.tsx` use richer JSON-parsing version | CODE REVIEW | STATIC PASS |

### UPLOAD-SKIP-1 — Idempotent duplicate skip for period inputs (backend)
| Check | Type | Result |
|---|---|---|
| TEST 1: First upload (1 new row) | LIVE | **PASS** — `{"created":1,"skipped":0,"errors":[]}` |
| TEST 2: Re-upload same row (duplicate) | LIVE | **PASS** — `{"created":0,"skipped":1,"errors":[]}` |
| TEST 3: Mixed batch (1 dup + 2 new, 3 rows) | LIVE | **PASS** — `{"created":2,"skipped":1,"errors":[]}` |
| TEST 4: Unknown `input_code` | LIVE | **PASS** — row-level error returned, not 500 |
| TEST 5: Negative `quantity` | LIVE | **PASS** — `{"detail":"quantity must be >= 0"}` |
| TEST 6: Non-numeric `quantity` (`"evil"`) | LIVE | **PASS** — `{"detail":"quantity must be a number"}` |
| TEST 7: 2001 rows (DoS cap) | LIVE | **PASS** — HTTP 400 `"Bulk upload limit is 2000 rows per request"` |

**Workspace used:** `79deffb3-394e-4045-843a-5fc3e6bc9e5d` (ACME AE)
**Employee used:** `SMC 1250` (Abayomi OGUNSUYI)
**Input code:** `accident_free`
**Cleanup:** All test rows deleted; workspace restored to pre-test state.

**Security notes verified during this session:**
- `IntegrityError` narrowed to `psycopg2.errors.UniqueViolation` — non-unique constraint violations log server-side and return a generic message (not `str(exc)`)
- Row count cap enforced before DB session opens (DoS mitigation)
- `isinstance(quantity, (int, float))` guards both `add_input` and `bulk_add_inputs` endpoints
- No `str(e)` returned in any response path (standing CLAUDE.md rule satisfied)

---

## WS-ACTIVATE Fix (WS-ACTIVATE-1/2/3)

| Check | Type | Result |
|---|---|---|
| `WorkspaceConfig.tsx` shows activation CTA when workspace not active | BLOCKED | UI only — verified live by operator during sprint |
| `PayrollRuns.tsx` shows activation CTA + disabled run button | BLOCKED | UI only — verified live by operator during sprint |
| `WorkspaceSetup.tsx` redirects to config on setup complete | BLOCKED | UI only — verified live by operator during sprint |

*Note: User confirmed live verification was performed during implementation and changes were accepted.*

---

## Data Integrity Spot-Check

| Check | Result | Notes |
|---|---|---|
| Null `net_pay` on CALCULATED runs | **PASS** (0) | |
| Null `component_trace_jsonb` on SUCCESS | **WARN (20)** | Pre-existing — all from Feb–Mar 2026, legacy executor runs predating Sprint 7. Not a regression from this sprint. |
| Duplicate statutory rules | **PASS** (0) | |
| Multiple active pay cycles per workspace | **PASS** (0) | |

---

## TypeScript
```
cd frontend && npx tsc --noEmit
# 0 errors, 0 warnings
```

---

## Known Pre-Existing Issues

| Issue | Count | First seen | Status |
|---|---|---|---|
| `component_trace_jsonb IS NULL` on legacy executor SUCCESS rows | 20 | ~Sprint 3–4 | Pre-existing. Legacy executor path (`executor.py`) does not populate trace. All affected runs are Feb–Mar 2026. Tracked in ROADMAP backlog. |

---

## Deferred

- **UPLOAD-ERR-1 LIVE verification** — requires Playwright or manual browser session; frontend-only rendering, no backend endpoint to curl. Operator verified live during implementation.
- **WS-ACTIVATE-1/2/3 LIVE verification** — same; UI-only surfaces.

---

## Sign-off
Verified by: Claude Code (automated) + operator live verification (frontend surfaces)
