# Sprint 10 Test Report — 2026-05-01

## Summary
| Metric | Value |
|---|---|
| Sprint | 10 (Client B Gap Closure) |
| Date | 2026-05-01 |
| Test suite | 226 passed, 4 failed (all pre-existing) |
| TypeScript | Clean — 0 errors |
| Code verifications | 12 sprint-item checks |
| Overall verdict | **PASS** (no new failures introduced) |

## Environment
- DB: `payroll_dev` — connected, migrations at head (`26b848abab55`)
- Duplicate revision ID warning: `a1b2c3d4e5f6` (pre-existing migration debt, tracked in memory)
- Backend/Frontend servers: not running during test run (test suite uses in-process DB)

## Sprint Items Verified

| Story | Check | Result | Notes |
|-------|-------|--------|-------|
| CB-1 (GAP-2) | Double PH subtraction removed from `payroll.py` | PASS | `expected_working_days = period_ctx.working_days` — PHs excluded by context builder, no second subtraction |
| CB-2 (GAP-5) | `CUSTOM` annualization = 12 in `_DEFAULT_ANNUALIZATION` | PASS | `_DEFAULT_ANNUALIZATION[PeriodType.CUSTOM] == 12` confirmed |
| CB-3 (WI-15) | `_resolve_inputs` handles list of event dicts | PASS | Lines 157–160: `isinstance(raw, list)` guard + `sum(float(e.get("quantity")))` |
| CB-6 (FIX-4) | `tax_bands` → Decimal in retry service | PASS | Lines 213–218: `Decimal(str(r[0/1/2]))` for all band fields |
| CB-7 (WI-04) | `component_source` fallback in `fixed_amount` handler | PASS | Lines 317–322: `if amount == Decimal("0") and component_source: amount = components.get(component_source)` |
| CB-10 (WI-05) | `ot_multiplier` in `RULE_TYPE_MAP` | PASS | Line 274: `'ot multiplier': 'ot_multiplier'` |
| CB-10 | 7th Excel sheet (Workspace Payroll Config) | PASS | `ph_mode` parsing present in parser |
| CB-10 | `warnings` returned from Excel parser | PASS | `warnings` key in parser return dict |
| CB-10 | Rate-code warning for `ot_multiplier` rules | PASS | Lines 303–308: warning added if `rate_code` absent |
| CB-11 (WI-06) | `upsert_workspace_payroll_config` called post-commit | PASS | Lines 574–589: best-effort call after `db.commit()`, wrapped in try/except |
| CB-11 (WI-11) | d3 fallback → `LEAVE_ABSORBS_PH` | PASS | `?? 'LEAVE_ABSORBS_PH'` in WorkspaceConfig.tsx |
| CB-11 (WI-11) | d4 fallback → `ABSENT_IS_DEDUCTIBLE` | PASS | `?? 'ABSENT_IS_DEDUCTIBLE'` in WorkspaceConfig.tsx |
| CB-12 | `PH_ADDITIVE` removed from TypeScript union | PASS | Not present in `payroll.ts` |
| CB-12 | `PH_ADDITIVE` removed from WorkspaceConfig dropdown | PASS | Not present in WorkspaceConfig.tsx |
| CB-12 | `PH_ADDITIVE` removed from WorkspaceSetup dropdown | PASS | Not present in WorkspaceSetup.tsx |
| CB-12 | Backend graceful fallback for `PH_ADDITIVE` | PASS | `payroll.py`: WARN log + treat as `LEAVE_ABSORBS_PH` |
| V1 (CB-4) | Cross-period isinstance guard | PASS | No code change needed (pre-verified) |
| V2 (CB-5) | NHF key `employee_rate` | PASS | No code change needed (pre-verified) |
| V3 (CB-6) | tax_bands Decimal in retry service | PASS | Fixed (same as CB-6 above) |
| V4 (CB-9) | Zero `ot_code` references | PASS | No normalisation code needed |
| V5 (CB-13) | PH GET/POST/DELETE routes live | PASS | Confirmed in workspace.py |

## Data Integrity Spot-Check

| Check | Result | Notes |
|-------|--------|-------|
| `null_net_pay` | 0 ✅ | No issues |
| `missing_trace_on_success` | 20 ⚠️ | Pre-existing: legacy executor runs before component_trace_jsonb was introduced (Sprint 0 era). Not a regression. |
| `duplicate_statutory_rules` | 2 groups ⚠️ | Pre-existing dev data: `NG/2000-01-01` (2 rows), `NULL/2000-01-01` (5 rows). NULL country_code rows are test fixtures. The G7 UNIQUE constraint was applied but these rows pre-date it. Not a regression. |
| `multi_active_pay_cycle` | 0 ✅ | No issues |

## Regression Suite

```
4 failed, 226 passed, 1 skipped, 41 warnings in 3.59s
```

All 4 failures are pre-existing and confirmed by reverting Sprint 10 changes with `git stash` — identical failures before and after Sprint 10.

## Known Pre-Existing Issues

| # | Test | Symptom | Root Cause | Roadmap Item |
|---|------|---------|------------|-------------|
| TF-3 | `test_payroll_approval_and_lock_e2e` | net_pay 590773.33 ≠ 578273.33 | NHF (₦12,500 = 2.5% × ₦500k basic) not deducted in test scenario — likely NHF toggle or key mismatch in dev fixture data. Pre-dates Sprint 10. | F1 / SR9 area |
| TF-4 | `test_full_payroll_pipeline_e2e` | Same assertion | Same root cause | F1 / SR9 area |
| TF-5 | `test_partial_payroll_run_e2e` | Same assertion | Same root cause | F1 / SR9 area |
| TF-6 | `test_payroll_retry_e2e` | Same assertion | Same root cause | F1 / SR9 area |

**Note:** TF-1 (`test_paid_transition_writes_audit_entry`) and TF-2 (`test_deduction_floored_at_zero`) previously documented in ROADMAP.md as pre-existing are now **PASSING** — they were fixed in a prior sprint. The roadmap Known Test Failures table should be updated to replace TF-1/TF-2 with TF-3 through TF-6.

**Common pattern for TF-3 through TF-6:** All 4 expect `net_pay = 578273.33` (i.e. NHF deducted). The engine produces `590773.33` (NHF absent). Difference = ₦12,500 = 2.5% of BASIC=₦500,000. This is an NHF fixture/toggle configuration issue in the test workspace setup, not in the calculation logic itself.

## TypeScript
```
npx tsc --noEmit — clean, 0 errors
```

## Deferred

- **Live API smoke tests:** Backend server was not running during this session. API endpoint verification (CB-11 commit handler, CB-1 AUTOMATIC mode end-to-end) cannot be done without a live server. Recommended: start `uvicorn backend.main:app` and re-run Step 3 checks.
- **CB-8 / WI-01 OT seed verification:** Requires live DB query `SELECT code, multiplier FROM rate_code_registry WHERE workspace_id IS NULL ORDER BY code` — open item from Sprint 10 planning.

## Sign-off
Verified by: Claude Code (automated) — 2026-05-01
