# Sprint 7 Test Report — 2026-04-14

## Summary

| Metric | Value |
|---|---|
| Sprint | 7 |
| Date | 2026-04-14 |
| Test suite | **230 passed, 1 skipped, 0 failures** |
| API verifications | 16 checks — all PASS |
| Overall verdict | **PASS** |

---

## Environment

| Check | Status |
|---|---|
| Backend | Running (`backend.api.main:app`, port 8000) |
| DB migrations | `f7a8b9c0d1e2` (current head) |
| Alembic branches | 3 heads — pre-existing migration debt, not Sprint 7 |

---

## Track F — New API Routes

| # | Endpoint | Test | Result |
|---|---|---|---|
| F1 | GET `/workspaces/{id}/payroll-config` | Returns existing config | **PASS** |
| F2 | PUT `/workspaces/{id}/payroll-config` | Creates versioned config; re-GET returns it | **PASS** |
| F3 | GET `/workspaces/{id}/rate-codes` | Returns platform seeds with `is_platform: true` | **PASS** |
| F4 | POST `/workspaces/{id}/rate-codes` | Creates workspace row | **PASS** |
| F4b | POST duplicate code | Returns 409 with message | **PASS** |
| F5 | DELETE workspace rate-code | Returns `ok` | **PASS** |
| F5b | DELETE platform seed | Returns 403 | **PASS** |
| F5c | DELETE non-existent | Returns 404 | **PASS** |
| F6 | GET `/workspaces/{id}/public-holidays?year=2026` | Returns 10 national holidays; `holiday_id: null`, `source: NATIONAL` | **PASS** |
| F7 | POST `/workspaces/{id}/public-holidays` | Created with `source: WORKSPACE` | **PASS** |
| F8 | DELETE workspace holiday | Returns `ok` | **PASS** |
| F8b | National holidays undeletable | `holiday_id: null` prevents delete | **PASS** |

---

## Track H — CSV Exports

| # | Endpoint | Test | Result |
|---|---|---|---|
| H-guard | Export from CALCULATED run | Returns 409 with status message | **PASS** |
| H1 | GET `.../exports/bank-upload` | Correct columns; bank name + account populated from biodata | **PASS** |
| H2 | GET `.../exports/paye` | TIN, gross_pay, paye_withheld, period (YYYY-MM) populated | **PASS** |
| H3 | GET `.../exports/pension` | RSA pin, basic_pay, pension_base, employee_contribution populated | **PASS** |

**H3 note:** `employer_contribution = 0.00` when `PENSION_EMPLOYER` component is not in the run's component metadata. This is expected — the code correctly handles missing trace entries. Affects workspaces that haven't registered `PENSION_EMPLOYER` as a component.

---

## Data Integrity Spot-Check

| Check | Count | Status |
|---|---|---|
| NULL net_pay on results | 0 | PASS |
| NULL trace on SUCCESS results | 20 | Pre-existing (legacy executor runs before Sprint 7) |
| Duplicate statutory rules | 2 | Pre-existing (unapplied constraint migration `d4e5f6a7b8c9`) |
| Multiple active pay cycles per workspace | 0 | PASS |

---

## Regression Suite

Full automated test suite: **230 passed, 1 skipped, 0 failures** (`python -m pytest tests/ -q`).

Core regression flows verified via test suite:
- Payroll run → CALCULATED → APPROVED → LOCKED → PAID lifecycle
- Retry of PARTIAL run
- Reconciliation (MATCHED / MISMATCH / RESOLVED)
- Run immutability after PAID
- Snapshot integrity
- Audit trail

---

## Known Pre-Existing Issues (Not Sprint 7 Regressions)

| Issue | Ref | Impact |
|---|---|---|
| Migration branch debt: 3 heads, duplicate `a1b2c3d4e5f6` revision | `project_migration_branch_debt.md` | Dev friction only — no runtime impact |
| 20 old SUCCESS rows with NULL `component_trace_jsonb` | Legacy executor, pre-Sprint 7 | No impact on new runs |
| Duplicate statutory rules (no UNIQUE constraint yet) | Unapplied migration `d4e5f6a7b8c9` | Risk of double-rule application if seeded again |

---

## Deferred

- **Track I — P2-2 (real user identity in `performed_by`)**: explicitly deferred at user request. No verification needed this sprint.

---

## Sign-off

Verified by: Claude Code (automated)
Sprint items covered: Tracks F, G (frontend — TypeScript compile only), H
Outstanding: Track I deferred
