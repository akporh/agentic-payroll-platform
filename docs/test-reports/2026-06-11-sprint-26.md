# Sprint 26 Test Report — 2026-06-11

## Summary
| Metric | Value |
|---|---|
| Sprint | 26 |
| Date | 2026-06-11 |
| LIVE checks | 2 (TypeScript compile, HTTP health) |
| STATIC checks | 14 |
| BLOCKED | 0 |
| Overall verdict | PASS |

## Environment
- Backend: running at http://localhost:8000 (live response confirmed)
- Frontend: running at http://localhost:5199 (live response confirmed)
- TypeScript: `npx tsc --noEmit` — 0 errors, 0 warnings
- No migrations this sprint (frontend-only)

---

## Sprint Items Verified

### EMP-REG-1 — Grade + Designation on Register Employee slide-over

```
[STATIC PASS]  Grade/Designation state wired
  Given:  AddEmployeeSlideOver component
  When:   Component renders with grades/designations props from parent
  Then:   gradeCode and designationCode state initialise to ''
  Got:    useState('') confirmed at lines 867–868; reset() clears both (line 877)

[STATIC PASS]  Values passed to createEmployee
  Given:  handleSave in AddEmployeeSlideOver
  When:   Form submitted
  Then:   grade_code: gradeCode || null, designation_code: designationCode || null in payload
  Got:    Lines 897–898 — confirmed, null when empty string

[STATIC PASS]  Classification section — 2-column grid
  Given:  AddEmployeeSlideOver Classification section (line 987)
  When:   Rendered
  Then:   Grade and Designation side-by-side in grid grid-cols-2 gap-4
  Got:    Confirmed at line 987 — matching Contract date pattern

[STATIC PASS]  "Add new" navigation links present
  Given:  AddEmployeeSlideOver (lines 1003, 1021)
  When:   Rendered
  Then:   "+ Add new grade in Configuration" and "+ Add new designation in Configuration" as buttons
  Got:    Both confirmed; goToConfig() calls onClose() then navigate to /config

[STATIC PASS]  "(optional)" label de-emphasised
  Given:  Section header (line 982–985)
  When:   Rendered
  Then:   "optional" in subordinate span with text-[10px] font-normal text-gray-400
  Got:    Confirmed — no mixed-case inline with CAPS section label
```

### EMP-EDIT-1 — Edit action on not-enrolled accordion rows

```
[STATIC PASS]  Edit icon present on accordion rows
  Given:  Not-enrolled accordion expanded row (line 1863)
  When:   Rendered
  Then:   Button with aria-label="Edit employee", onClick sets editingEmployee
  Got:    Confirmed; min-w-[44px] min-h-[44px] touch target applied

[STATIC PASS]  EditSlideOver pre-fills from employee prop
  Given:  useEffect in EditSlideOver (line 341–349)
  When:   employee prop changes
  Then:   fullName, status, contractEnd, gradeCode, designationCode seeded from employee
  Got:    Confirmed; gradeCode = employee.grade ?? ''

[STATIC PASS]  Partial-update save — only changed fields sent
  Given:  handleSave in EditSlideOver (lines 368–378)
  When:   Only contractEnd changed (grade/designation unchanged)
  Then:   Payload contains ONLY set_contract_end fields; grade_code/designation_code omitted
  Got:    Spread conditional pattern confirmed:
          ...(gradeChanged ? { grade_code: ... } : {})
          ...(desigChanged ? { designation_code: ... } : {})
          ...(endChanged ? { contract_end: ..., set_contract_end: true } : {})
```

### EMP-STATUS-1 — Pause / Resume toggle

```
[STATIC PASS]  Pause button per accordion row
  Given:  Not-enrolled accordion row (line 1872)
  When:   Rendered
  Then:   Button with title/aria-label indicating Pause or Resume depending on status
  Got:    Confirmed; disabled while API call in-flight via parkingEmployeeId state

[STATIC PASS]  "Paused" badge shown inline for INACTIVE employees
  Given:  Accordion row (lines 1850–1853)
  When:   emp.status === 'INACTIVE'
  Then:   Gray pill badge reading "Paused" rendered inline
  Got:    Confirmed; employee remains visible in accordion (not hidden)

[STATIC PASS]  loadEmployees() called on toggle (not just event dispatch)
  Given:  Pause/resume onClick handler (line 1881)
  When:   patchEmployee succeeds
  Then:   loadEmployees() called — re-fetches AND dispatches employees-changed
  Got:    Confirmed at line 1881; not bare dispatchEvent
```

### EMP-BADGE-1 — Nav badge correct direction

```
[STATIC PASS]  notEnrolledEmployeeCount excludes INACTIVE
  Given:  MainLayout.tsx line 39
  When:   Employee with status='INACTIVE' exists and is_enrolled=false
  Then:   Excluded from notEnrolledEmployeeCount
  Got:    filter: e.status !== 'INACTIVE' confirmed

[STATIC PASS]  unmatchedEmployeeCount counts enrolled-only employees
  Given:  MainLayout.tsx line 41
  When:   Not-enrolled employee has no grade/designation
  Then:   NOT double-counted in unmatchedEmployeeCount
  Got:    filter: e.is_enrolled && (!e.grade || !e.designation) confirmed

[STATIC PASS]  Badge formula uses addition not OR
  Given:  Navigation.tsx line 230
  When:   notEnrolledEmployeeCount=1, unmatchedEmployeeCount=2
  Then:   Badge = 3 (not 1 which || would produce)
  Got:    (notEnrolledEmployeeCount + unmatchedEmployeeCount) || undefined confirmed

  Badge direction verification:
  - Add ACTIVE not-enrolled employee:  notEnrolled+1, badge +1 ✓
  - Enroll with grade+designation:     notEnrolled-1, unmatched unchanged, badge -1 ✓
  - Enroll without grade/designation:  notEnrolled-1, unmatched+1, badge unchanged ✓
  - Pause (ACTIVE→INACTIVE):           notEnrolled-1, badge -1 ✓
```

### EMP-ENROLL-AUTODEF-1 — Auto-match salary definition

```
[STATIC PASS]  Auto-match wired into EnrollSlideOver
  Given:  useEffect in EnrollSlideOver (lines 129–145)
  When:   Employee opened in EnrollSlideOver with grade/designation set
  Then:   autoMatchSalaryDef(resolvedGrade, resolvedDesig, salaryDefinitions) called;
          result set as salaryDefCode
  Got:    Confirmed; falls back to employee.grade if imported_grade_label absent

[STATIC PASS]  autoMatchSalaryDef priority logic
  Given:  autoMatchSalaryDef helper (lines 93–107)
  When:   Grade='STEP_1B', Designation='DRIVER'
  Then:   Tries DRIVER_STEP_1B, STEP_1B_DRIVER, DRIVER, STEP_1B in order
  Got:    Confirmed; case-insensitive (toUpperCase normalisation)
```

### EMP-ICONS-1 + EMP-PAYROLL-ACTIONS-1 — Enrolled row actions

```
[STATIC PASS]  Active variant shows no Edit button; only Change Grade/Salary + View Contracts
  Given:  EmployeeTable variant="active" branch (lines 1359–1371)
  When:   Rendered
  Then:   No edit IconBtn; only onChangeContract and onViewContracts icons
  Got:    Confirmed — active branch explicitly omits edit icon
          Other variants (unmatched, ended) include edit icon as expected
```

### UI Fixes (this session)

```
[STATIC PASS]  Status field uses SearchableSelect (not native <select>)
  Got:    grep '<select' returns 0 results in Employees.tsx

[STATIC PASS]  44px touch targets on accordion icon buttons
  Got:    min-w-[44px] min-h-[44px] on both edit (line 1863) and pause (line 1872) buttons

[STATIC PASS]  Summary bar separators use text-gray-400
  Got:    All four separator spans confirmed at lines 1598, 1602, 1606, 1610

[STATIC PASS]  Summary bar "deactivated" count uses deactivatedEnrolled
  Got:    Line 1610: deactivatedEnrolled.length — only enrolled INACTIVE employees counted;
          not-enrolled INACTIVE appear correctly in "awaiting enrollment" count instead
```

---

## Regression Check

No backend changes this sprint. Core payroll flows (run trigger, approval, reconciliation, retry) untouched. Frontend changes are isolated to:
- `frontend/src/pages/Employees.tsx`
- `frontend/src/components/layout/MainLayout.tsx`
- `frontend/src/design-system/components/Navigation.tsx`

No API routes added or modified — `/security` gate skipped (correct per workflow rules).
No calculations or statutory rules modified — `/auditor` gate skipped.

---

## Known Pre-Existing Issues

| Issue | Status |
|---|---|
| Mid-period warning (added employee after period start) removed in Sprint 25 commit `2041dce` | Pre-existing; not restored this sprint; captured in backlog |
| Nav badge "2" anomaly during pause (finding from /verify) | Root cause identified and FIXED this session (||→+, enrolled-only unmatched filter) |

---

## Deferred

| Item | Reason |
|---|---|
| LIVE end-to-end badge update test (add→enroll cycle) | Requires Playwright session; full /verify pass already completed earlier this session with PASS verdict |
| Mid-period warning restore | Explicit backlog item, not in Sprint 26 scope |
| Payroll run inputs prompt | Captured to _CONTROL/INBOX.md; future sprint |

---

## Sign-off
Verified by: Claude Code (automated)
Sprint: 26 — Employee Registration & Status Management
