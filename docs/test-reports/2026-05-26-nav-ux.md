# Nav UX Fixes — Test Report 2026-05-26

## Summary
| Metric | Value |
|---|---|
| Change | Nav order + employee mismatch badge |
| Date | 2026-05-26 |
| TypeScript | PASS (0 errors) |
| Static checks | 4 passed |
| Live UI checks | BLOCKED (servers offline) |
| Overall verdict | PASS (static) / BLOCKED (live UI) |

## Environment
- Backend: offline (not started this session)
- Frontend dev server: offline (not started this session)
- TypeScript (`npx tsc --noEmit`): clean, 0 errors

## Changes Verified

### Change 1 — Inputs above Runs in sidebar nav

| Check | Result | Evidence |
|---|---|---|
| Inputs item appears before Runs item in `sections` array | PASS | `Navigation.tsx:219-220` — Inputs first, Runs second |
| TypeScript compiles cleanly | PASS | `npx tsc --noEmit` — no errors |

**Given:** `WorkspaceSidebar` builds its Payroll section items array  
**When:** The sections array is constructed  
**Then:** Inputs (`/payroll/inputs`) is listed at index 0, Runs (`/payroll`) at index 1  
**Got:** Confirmed at lines 219–220 of `Navigation.tsx`

---

### Change 2 — Employee mismatch badge on sidebar Employees nav item

| Check | Result | Evidence |
|---|---|---|
| `SidebarItem` interface has `badge?: number` field | PASS | `Navigation.tsx:199` |
| `WorkspaceSidebarProps` has `unmatchedEmployeeCount?: number` | PASS | `Navigation.tsx:209` |
| Badge renders only when `!collapsed && item.badge` | PASS | `Navigation.tsx:286` — badge suppressed in collapsed state |
| Collapsed tooltip includes count when badge > 0 | PASS | `Navigation.tsx:282` — `"Employees (N unmatched)"` in title |
| Badge capped at `99+` for large counts | PASS | `Navigation.tsx:291` |
| `PageShellProps` threads `unmatchedEmployeeCount` | PASS | `Layout.tsx:39, 55, 80` |
| `MainLayout` fetches employees on `workspaceId` change | PASS | `MainLayout.tsx:20-23` |
| `MainLayout` filter matches `Employees.tsx` filter exactly | PASS | Both use `!e.is_ended && (!e.grade \|\| !e.designation)` |
| Employee fetch cleared when no workspace | PASS | `MainLayout.tsx:21` — guard `if (!workspaceId) { setEmployees([]); return; }` |
| `Employee` type has `is_ended`, `grade`, `designation` fields | PASS | `types/payroll.ts:51-55` |

**Filter equivalence check:**
- `Employees.tsx:637`: `active.filter((e) => !e.grade || !e.designation)` where `active = employees.filter((e) => !e.is_ended)`
- `MainLayout.tsx:26`: `employees.filter(e => !e.is_ended && (!e.grade || !e.designation))`
- Logically identical — same count will appear in banner and badge.

---

## BLOCKED — Live UI Checks

These require running servers and cannot be verified statically:

| Check | Blocker | How to verify |
|---|---|---|
| Badge renders visually at correct position (right-aligned in nav item) | Servers offline | Start frontend, navigate to a workspace, confirm amber badge appears next to "Employees" |
| Badge disappears when all employees are matched | Servers offline | Assign grade+designation to all unmatched employees; badge should vanish |
| Badge count matches AlertBanner count on Employees page | Servers offline | Compare badge number to banner headline |
| Badge hidden when sidebar collapsed | Servers offline | Collapse sidebar, confirm no badge overflow on icon |
| Collapsed tooltip shows `"Employees (N unmatched)"` | Servers offline | Hover icon while collapsed, read tooltip |
| Inputs tab active state fires on `/payroll/inputs` route | Servers offline | Navigate to Inputs; confirm it highlights, not Runs |

**To unblock:** `cd backend && uvicorn main:app --reload` and `cd frontend && npm run dev`

## Deferred
- No regression suite run (no sprint items touched calculation logic, migrations, or API routes — standard regression checklist not applicable to this change)
- No data integrity spot-checks required (pure frontend change, no DB writes)

## Sign-off
Verified by: Claude Code (automated static analysis)  
Live UI verification: BLOCKED — pending server start
