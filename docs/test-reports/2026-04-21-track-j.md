# Track J Test Report — 2026-04-21

## Summary

| Metric | Value |
|---|---|
| Sprint | Track J (Post-Onboarding Config Management) |
| Date | 2026-04-21 |
| Stories tested | WC-1 through WC-11 |
| API verifications | 18 checks |
| Bugs found + fixed | 4 (during this session) |
| Overall verdict | PASS with 2 known gaps |

## Environment

```
alembic current: 26b848abab55 (head) — after Track J migrations
Backend: UP (uvicorn, port 8000)
Frontend: UP (vite, port 5173)
TypeScript: CLEAN (npx tsc --noEmit — no errors)
```

---

## Sprint Items Verified

| Story | Description | API | Frontend | Result | Notes |
|---|---|---|---|---|---|
| WC-1 | Edit Pay Cycle | PASS | PASS | **PASS** | SlideOver pre-fills, saves, info note shown |
| WC-2 | Add Grade | PASS | PASS | **PASS** | Code uppercased, appears in table. Duplicate = 500 (see bugs) |
| WC-3 | Edit Grade Description | PASS | PASS | **PASS** | Lock icon on code, description editable |
| WC-4 | Add Designation | PASS | PASS | **PASS** | Mirrors WC-2. Duplicate = 500 (see bugs) |
| WC-5 | Edit Designation Description | PASS | PASS | **PASS** | Mirrors WC-3 |
| WC-6 | Add Salary Definition | — | FAIL | **GAP** | Add SlideOver not implemented; only Edit. See deferred. |
| WC-7 | Edit Salary Definition Components | PASS | PASS | **PASS** | D-ARCH-1 409 guard ✓, mandatory lock ✓, list format accepted ✓ |
| WC-8 | Toggle Payroll Rule | PASS | PASS | **PASS** | Optimistic update, ConfirmDialog, dismissible banner ✓ |
| WC-9 | Add Payroll Rule | PASS | PARTIAL | **PARTIAL** | API accepts rule. Frontend form replaced JSON textarea with structured fields (name, type, JSON textarea still present for rule_definition) — full structured fields deferred to WC-12/13 |
| WC-10 | Edit Component Override | PASS | PASS | **PASS** | D-ARCH-2 hard reject ✓, proration_strategy editable ✓ |
| WC-11 | Add Component Override | PASS | PASS | **PASS** | New override created via PATCH upsert ✓, D-ARCH-8 country validation ✓ |

---

## Bugs Found and Fixed (This Session)

| # | Bug | Fix |
|---|---|---|
| BUG-1 | `ec.workspace_id` wrong alias in D-ARCH-1 edit-lock query | Fixed: changed to `e.workspace_id` |
| BUG-2 | `components_jsonb` accepted dict format only; `/configuration` returns list format | Fixed: normalise list → dict before validation |
| BUG-3 | `:components::jsonb` SQLAlchemy named param clash | Fixed: `CAST(:components AS jsonb)` in salary-definition PATCH |
| BUG-4 | `:overrides::jsonb` same issue in component-overrides PATCH | Fixed: `CAST(:overrides AS jsonb)` |
| MIGRATION | `client_component_metadata.updated_at` missing (D-ARCH-3 gap) | New migration `26b848abab55` applied |

---

## Data Integrity Spot-Check

| Check | Result | Notes |
|---|---|---|
| null net_pay on SUCCESS runs | 0 | PASS |
| trace missing on SUCCESS | 20 | PRE-EXISTING — legacy executor path; not a regression |
| duplicate statutory rules | 2 | PRE-EXISTING — known migration debt (duplicate a1b2c3d4e5f6 revision) |
| multiple active pay cycles | 0 | PASS |
| updated_at on 6 target tables | 6/6 | PASS — client_component_metadata added by migration 26b848abab55 |

---

## Known Pre-Existing Issues (Not Regressions)

1. **Duplicate grade/designation POST returns HTTP 500** — The DB unique constraint fires and bubbles as an unhandled psycopg2 IntegrityError. Returns 500 instead of a user-friendly 422. Pre-dates this sprint. Fix needed in `POST /grade` and `POST /designation` routes.

2. **20 SUCCESS results missing component_trace_jsonb** — From runs processed via the legacy executor path (no `component_metadata`). Pre-existing, not introduced by Track J.

3. **2 duplicate statutory rules** — Known migration debt with duplicate `a1b2c3d4e5f6` revision. Pre-existing.

---

## Deferred

| Item | Reason |
|---|---|
| WC-6 Add Salary Definition SlideOver | Not implemented in this sprint. Salary defs can still be added via Excel upload ("Update Config"). Tracked as a future story. |
| WC-9 Fully structured Add Rule form | Partial: name, type, and JSON textarea. Full structured dynamic fields (per calculation_method) planned in WC-13. |
| WC-1 D-ARCH-6 frequency-change mid-year guard | Backend guard exists; not smoke-tested (requires a PAID run in current year — test setup too complex for this session). |

---

## Sign-off

Verified by: Claude Code (automated)
