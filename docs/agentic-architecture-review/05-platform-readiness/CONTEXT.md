# Stage 05: Platform Readiness — Context

## Status
not-started

## Binding decisions inherited from Stage 02 (pre-scope — do not re-litigate)

Recorded 2026-07-12, full detail in `_core/HUMAN-DECISIONS.md` HD-2–HD-5:

- **D-02-02**: `payroll_reconciliation` repository-level workspace-scoping fix (F-01-33) is a **named precondition** for any reconciliation-reading agent tool — not an open risk-acceptance question.
- **D-02-03**: closing F-01-27 (salary_definition editable pre-PAID), F-01-29 (trace fallback ambiguity), F-01-38 (dead status branches in the D-ARCH-1 guard) is a **named launch precondition** for historical-outcome-explaining/investigating agents — explicitly not an accepted residual risk to disclose to operators instead.
- **D-02-04**: a statutory-rule change-management mechanism (beyond migration-only maintenance, F-01-45/46) is scoped as its own deterministic platform capability, independent of any AI compliance-detection agent.

## Scope

_To be defined when this stage is opened. Do not populate ahead of the gate — scope defined in advance of the prior stage closing risks anchoring on assumptions instead of the prior stage's confirmed findings. The binding decisions above are constraints, not the scope itself._

## Questions this stage answers

_To be defined at stage start._

## Explicitly out of scope

_To be defined at stage start._

## Inputs consumed

_Confirmed findings (F-) from prior gated-closed stages, plus any new sources — recorded in `_inputs/source-register.md`._

## Next action

**Await approval to begin Stage 05 (Stages 03 and 04 come first).**
