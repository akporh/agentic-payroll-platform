# Human Decisions Log

Records decisions in this audit programme that require Michael's judgment
rather than more evidence — risk acceptance, scope calls, and approval to
proceed on anything outside a stage's default read-only, evidence-gathering
remit.

Logged here, not in a stage's `findings.md`, because these are decisions
*about* findings, not findings themselves.

## When an entry is required

- Any finding reaching **S0 — Critical** at `plausible` or `confirmed`
  status (per `severity-model.md` escalation rule).
- Any case where a stage is blocked because required evidence cannot be
  obtained without touching `backend/`, `frontend/`, or `migrations/`.
- Approval to open a stage before its declared dependency has reached
  `complete`.
- Approval of the Stage 13 backlog itself, and of the decision to begin
  production-code remediation afterward (which happens outside this
  workspace, as an ordinary sprint).
- Any decision to mark a stage `complete` with open, unresolved findings
  (i.e., accepting residual risk rather than blocking).

## Log

| Date | Decision needed | Context | Options | Decision | Decided by |
|---|---|---|---|---|---|
| 2026-07-11 | Is an empty `component_metadata` list meant to silently trigger the legacy executor fallback, same as an absent one? | Finding 01-004: `backend/api/routes/payroll.py:866` coerces `[]` to `None` before calling into `execute_single_employee_payroll`; `executor.py:108`'s `if component_metadata:` check treats `None` and `[]` identically. `CLAUDE.md`'s Executor Paths note only describes the `None` case and separately directs "migrate all callers" away from the legacy path. | (a) Intended — a workspace with no configured components should fall back silently; (b) Unintended — should instead surface as an onboarding-incomplete error; (c) Defer classification to Stage 02 (execution trace baseline), which will characterize how often this actually fires in practice | pending | — |
| 2026-07-11 | Is `backend/infra/db/repositories/workspace_repo.py` an intentional second repository layer, or migration debt? | Finding 01-002: this ORM-based file is imported only by three onboarding-domain modules, separate from the 14-file raw-SQL `backend/infra/repositories/` layer that `CLAUDE.md`'s architecture table describes as the sole repository location. | (a) Intentional isolation of onboarding-status checks; (b) Incomplete migration — should be consolidated into `backend/infra/repositories/`; (c) Leave open, revisit if a later stage (e.g. Stage 06 wiring) surfaces a concrete problem caused by the split | pending | — |

| 2026-07-11 | Is `docs/wrapper-command/` (an agent-instruction set addressed to "Casper," covering engineering-playbook rules, architecture-check prompts, and a payroll "SYSTEM GUARD") still authoritative, and how does it relate to `CLAUDE.md`? | Finding 01-013: this instruction set was not surfaced in the audit programme's original repository orientation and was found only during continued Stage 01 execution. Its currency, ownership, and relationship to `CLAUDE.md` (which this audit programme treats as the sole governing instruction source) is unknown. | (a) Superseded by `CLAUDE.md` — archive or mark deprecated; (b) Still in active use alongside `CLAUDE.md` for a distinct purpose ("Casper" tooling) — document the relationship; (c) Unknown — treat as non-authoritative for now, no further classification | **(c) Unknown — treat as non-authoritative for now.** "Casper" confirmed to be Michael's name for his coding agent (i.e. this agent), not a separate third-party tool — but no content-level comparison against `CLAUDE.md` was performed, and none is authorized at this time. `docs/wrapper-command/` must not be cited as an authoritative source in any later audit stage; `CLAUDE.md` remains the sole governing instruction source. | Michael |

**Resolved 2026-07-11.** The other two entries remain pending — they are recorded per the escalation rule so they are visible ahead of later stages rather than re-discovered.

| 2026-07-11 | Should per-employee retry produce the same `execution_trace` step-level audit footprint as an original run? | Finding 02-002 (Stage 02): `payroll_retry_service.py::retry_failed_payroll_employees` instantiates an `ExecutionTracer` but never calls `.step(...)`, so retries write zero rows to `execution_trace`, while original runs write ~7–9. `component_trace_jsonb` (per-employee, per-component) is unaffected — only the coarser run-level step trace is missing for retries. | (a) Acceptable as-is — `payroll_result.status`/`error_message` plus audit_log/event_store already capture retry outcomes at the row level; (b) Add `tracer.step(...)` wrapping to retry so it mirrors the original run's step trace; (c) Defer to Stage 04 (original-run and retry parity), which depends on this Stage 02 baseline and is the next stage scoped to compare original-vs-retry behaviour in depth | pending | — |
| 2026-07-11 | Should `export_payroll_register_csv` (and its siblings `export_net_pay_csv`, `export_paye_summary_csv`) be fixed and wired up, or retired? | Finding 02-009 (Stage 02): `export_payroll_register_csv` assumes `gross_components_jsonb` is a `list[dict]`; production (`executor.py:343-347`) writes it as `dict[str, dict]`. The function has zero production callers today, so this cannot currently corrupt a live export, but was already flagged as a backlog gap in prior analysis (`project_sprint6_backlog.md` memory: P0-3/P1-4/P1-5). Its own test script (`backend/scripts/test_export_register.py`) uses dummy data in the old list shape, masking the mismatch. | (a) Fix the shape mismatch and wire the export into an API route as part of closing the export gap; (b) Retire these three export functions if exports are being redesigned differently; (c) Leave open for Stage 13's consolidated backlog to prioritize alongside the other export gaps | pending | — |
| 2026-07-12 | Should per-employee retry read the frozen `rules_context_snapshot.statutory_rule` instead of re-resolving the statutory rule/tax bands live? | Finding 03-002 (Stage 03): `payroll_retry_service.py::_build_shared_context` re-queries live `statutory_rule`/`tax_band` by `country_code` + frozen `statutory_effective_date`, even though the original run already froze the exact resolved statutory content (`id`, `version`, `rules_jsonb`, `tax_bands`) into `rules_context_snapshot.statutory_rule` and that key is never read. If a new `statutory_rule` row is inserted with an `effective_from` between the date originally resolved and the run's `statutory_effective_date`, retry could silently compute PAYE/pension/NHF/health/levy differently for retried employees than the original run did for their successful peers — no error, no warning, no trace entry. Not yet confirmed with a controlled test. | (a) Fix retry to read `original_snapshot["statutory_rule"]` directly (mirrors how `rule_set_item` and public holidays already work correctly); (b) Confirm via controlled test whether this has ever actually fired in practice before prioritizing a fix; (c) Defer to Stage 04 (original-run and retry parity), which is scoped to reproduce exactly this kind of divergence | pending | — |
| 2026-07-12 | Is `employee_contract_snapshot.components_jsonb` meant to ever be read, or is it dead storage to remove? | Finding 03-003 (Stage 03): the column is written at every run's snapshot creation but has zero readers anywhere in `backend/` — retry explicitly reads salary amounts from a live join instead (by design, D1), mitigated from drift risk by the D-ARCH-1 edit-lock. Not a correctness defect today, but unclear if the column was meant to back a future audit/diff feature. | (a) Confirm it is intentionally unused and mark it for removal in a future Stage 12 simplification pass; (b) Identify a planned consumer (e.g. an audit view comparing frozen-vs-live salary) and keep it; (c) Leave open, no action needed | pending | — |
| 2026-07-12 | Is it still intended that any component — including statutory deductions like NHF, pension, or PAYE-adjacent components — can be disabled per workspace via `client_component_metadata.is_active`? | Finding 03-004 (Stage 03): `patch_component_override`'s own docstring lists a "D-ARCH-2: Statutory deduction components cannot be disabled" guard, but the guard is explicitly commented out ("intentionally not enforced for now... Re-enable this block to restore the restriction in future"). This is a documented, deliberate relaxation, not an oversight, but it is not reflected in `CLAUDE.md`'s Known Data Contract Rules table. | (a) Confirm the permissive behaviour is correct and add it to `CLAUDE.md`'s invariant table so it isn't rediscovered as a surprise later; (b) Re-enable the D-ARCH-2 guard; (c) Leave for Stage 09 (security/tenant isolation) or Stage 13 to assess actual production impact first | pending | — |

| 2026-07-12 | Should 05-001 (snapshot creation can fail silently) and 05-004 (inconsistent immutability enforcement across snapshot tables) be bundled into the same immediate remediation sprint as `04-001`, or deferred to Stage 13's normal backlog? | Findings 05-001/05-004 (Stage 05): both are confirmed S2 gaps discovered while validating the 04-001 fix's snapshot contract. Neither causes incorrect calculation today (unlike 04-001), but 05-001 can leave a run permanently stuck without operator-visible cause, and 05-004 means only 2 of ~8 snapshot mechanisms are DB-protected against future accidental mutation. The remediation sprint will already be touching `payroll_retry_service.py` and the snapshot write paths for the 04-001 fix. | (a) Bundle both into the same sprint — low marginal cost given the sprint is already touching this code; (b) Fix only 04-001 now, defer 05-001/05-004 to Stage 13's normal backlog; (c) Bundle 05-004's trigger additions only (cheap, pure defense-in-depth) and defer 05-001 (requires a design decision on retry-vs-surface-vs-alert behaviour) | **Split decision, recorded 2026-07-12 at Stage 05 close:** (1) `05-001` IS bundled into the immediate remediation sprint with `04-001` — snapshot creation is part of the retry guarantee, and a silent background-task failure can leave a completed run permanently non-retryable with no operator-visible cause; the remediation must make this failure fail visibly and prevent the run from proceeding as successfully retryable when the required snapshot was not persisted. (2) `05-004` (the broad cross-snapshot immutability-trigger harmonisation) is NOT included in this sprint — deferred to Stage 13, with Stage 12 input where relevant, because no current update path mutates the unprotected snapshot tables and `payroll_run.rules_context_snapshot` (the specific snapshot the 04-001 fix depends on) already has DB-level immutability enforcement. Exception: if implementing 04-001 or 05-001 introduces or changes any snapshot mutation path, that path must preserve or strengthen existing immutability guarantees, never weaken them. (3) The 04-001 remediation specification (Stage 05 §9) is approved as ready for implementation. Remediation sprint scope: 04-001 + 05-001, before Stage 06 or any live payroll processing/production release. | Michael |

| 2026-07-12 | Is `GET /workspaces/{workspace_id}/timesheet/audit/{employee_id}` intentionally operator/API-only, or a planned-but-unbuilt UI feature? | Finding 06-006 (Stage 06): confirmed zero frontend callers for this route; no other context found to determine intent either way. | (a) Confirm intentionally operator/API-only, document as such; (b) Treat as a missing UI feature, scope a small follow-up; (c) Leave open, low priority | **(b) Resolved 2026-07-12 at Stage 06 close:** missing UI feature, not intentionally API-only. It supports a core bureau-operator workflow (explaining how uploaded timesheet data was interpreted into payroll inputs), belonging alongside the already-wired timesheet upload, payroll-result trace, reconciliation, and audit-history surfaces. The backend endpoint is retained as the correct source; the UI requirement is recorded for Stage 13 rather than implemented in this read-only stage. | Michael |

| 2026-07-12 | Should the systemic `str(e)`-leak pattern (07-001, Stage 07) be fixed as a dedicated, prioritized follow-up given it violates a standing `CLAUDE.md` rule already documented as recurring in Sprint 10 and Sprint 17, or flow through Stage 13's normal backlog ordering? | Finding 07-001 (Stage 07): 21 confirmed occurrences across `payroll.py` (7) and `workspace.py` (14) of `except Exception as e/exc: raise HTTPException(..., detail=str(e))`, some wrapping raw DB writes that can leak table/column/constraint names. This is the third documented occurrence of this exact defect class in this codebase's history, and by far the largest. | (a) Treat as a dedicated priority fix — a recurring, named standing-rule violation at this scale warrants breaking out of normal backlog ordering; (b) Flow through Stage 13 with its S1 severity naturally prioritizing it; (c) Bundle with Stage 09's broader security review of the same 21 sites, since distinguishing genuinely-unsafe from currently-harmless sites requires that level of review anyway | pending | — |

| 2026-07-12 | What is the intended `execution_trace` parity level for per-employee retry — full step-by-step parity with the original run, a defined minimal subset, or none beyond what already exists (zero rows)? | Finding 07-005 / carried from Stage 02's `02-002` (Stage 07): retry instantiates `ExecutionTracer` but calls `.step(...)` nowhere in its ~300-line body, confirmed unchanged since Stage 02 and unaffected by the `04-001`/`05-001` remediation. `component_trace_jsonb` (fine-grained, per-component) is unaffected; only the coarser run-level step trace is missing. No source anywhere in this codebase or its accumulated audit findings stated the intended parity level. Blocks Stage 10's remediation design. | (a) Full step-by-step parity with the original run; (b) A defined minimal subset (invocation/preflight outcome, per-employee outcome, final transition); (c) No change — zero rows remains acceptable, since `payroll_result.status`/`error_message` and audit/event already capture retry outcomes at the row level | **Resolved 2026-07-12 at Stage 07 close: (b) defined minimal subset.** Retry must produce: (1) one persisted row per retry invocation recording start and preflight outcome, including whether the `04-001` snapshot/statutory validation passed or failed; (2) one persisted row per retried employee recording success or failure and the employee identifier; (3) one final persisted row recording the resulting run transition (`PARTIAL → CALCULATED` or `PARTIAL → PARTIAL`). `component_trace_jsonb` remains the authoritative fine-grained calculation trace; `execution_trace` is scoped to orchestration, preflight, and outcome, not a duplicate of every original-run batching/persistence step, unless Stage 10 later identifies a concrete audit requirement for more. This is a decision, not an implementation — the design is deferred to Stage 10. | Michael |

| 2026-07-12 | **S0 ESCALATION — RESOLVED AT STAGE 04 CLOSE.** Finding `04-001` (Stage 04): the original-run/retry statutory-rule divergence hypothesized in `03-002` was **reproduced** by controlled non-production test (`docs/audit-program/04-original-run-retry-parity/evidence/statutory_divergence_controlled_test.py`, output in the same folder). A workspace with a `PARTIAL` run, where a new `statutory_rule` row is inserted with an `effective_from` between the run's original resolution and its frozen `statutory_effective_date`, will have any subsequently-retried employee taxed under the NEW rule while their already-successful peers in the SAME run were taxed under the OLD rule — silently, with no error, warning, or trace entry. Test used a flat-rate marker (10% vs. 25%) to make the divergence unambiguous: retried employee's PAYE matched the new rule almost exactly (₦92,000 actual vs. ₦92,000.00 expected under the new rule, vs. ₦36,800.00 expected under the original rule). This is a confirmed statutory/financial miscalculation mechanism per `_core/severity-model.md`'s S0 definition. | How urgently should this be fixed, and what is the canonical fix direction? | (a) Treat as urgent — fix ahead of Stage 13; (b) Standard priority — flow through Stage 13; (c) Assess likelihood first | **Six decisions recorded 2026-07-12, at Stage 04 close:** (1) `04-001` is a confirmed S0 release blocker. (2) The platform is not currently live, so this does not trigger an emergency mid-audit patch — the audit continues in sequence. (3) Remediation planning does not wait for Stage 13 — Stage 05 must first validate the snapshot contract and define the canonical fix; `04-001` then moves into an immediate remediation sprint after Stage 05's design work, and before any live payroll processing or production release. (4) Canonical fix direction: retry must consume the frozen statutory-rule content at `payroll_run.rules_context_snapshot["statutory_rule"]`, not re-resolve mutable live `statutory_rule`/`tax_band` tables. (5) The fix must preserve legacy-run compatibility explicitly — a run lacking the required frozen v2 statutory content must hard-fail with a clear correction-run instruction (mirroring `validate_snapshot_complete()`'s existing pattern), never silently fall back to a live re-query. (6) `04-002` remains a separate S1 observability finding — Stage 05's remediation design should record which statutory rule/version was actually used per calculation, or make that identity directly derivable from immutable persisted data. | Michael |

| 2026-07-12 | **S0 ESCALATION.** Is application-level authentication currently out of scope by design (e.g. network-level access control assumed to exist outside this repository), or is finding `09-000` (Stage 09) an unrecognized gap that must be closed before any production/live-data use? | Finding `09-000` (Stage 09): no authentication mechanism exists anywhere in `backend/` or `frontend/` — no token/session validation, no `current_user` concept, no auth dependency on any router, including `/admin*` operator dashboards and payroll approve/lock/pay routes. `workspace_id` is a plain client-supplied string with no identity to verify it against. CORS defaults `allow_origins=["*"]`. This is the root cause underlying nearly every other finding in Stage 09 (`09-001` through `09-008`). | (a) Confirm auth is intentionally deferred for this MVP/single-client deployment because access is controlled at the network layer outside this repository — document that assumption explicitly; (b) Treat as an unrecognized S0 gap that blocks production/live-data use until fixed; (c) Assess deployment reachability first before deciding | pending | — |
| 2026-07-12 | What is the intended role/tenancy model for this platform — a single bureau operator acting across all client workspaces, multiple workspace-scoped client users, or something else? | Finding `09-000`/Stage 09 §10 (privileged lifecycle-operation role matrix): `CLAUDE.md`'s "workspace scoping enforced at the query level" rule implies a multi-tenant intent, but no code anywhere implements "who is asking" — every operation (create/retry/approve/lock/pay/reconcile/export/view-audit) is reachable by any caller with no role distinction. Without this decision, Stage 10's remediation design for `09-000`/`09-002` cannot specify what "correct" authorization should look like. | (a) Single bureau operator model — one authenticated internal user acts across all client workspaces, workspace_id is a data selector not a security boundary; (b) Multi-tenant model — each client workspace has its own authenticated user(s), workspace membership must be verified server-side per request; (c) Hybrid — internal bureau operators plus client-facing read-only users, distinct role sets | pending | — |
