# Critic Review — Phase 4B Confirmed-Story Batch

**Reviewer:** independent read-only critic
**Scope reviewed:** `historical-migration-confirmed-batch-run-001.md`, `docs/product/` (all files), `docs/programmes/product-traceability/{decisions.md,PHASES.md,state.md,phase-inputs.yaml,exceptions.md}`, `docs/product/validate_registry.py`, all 19 new story files, cited evidence files, cited git commits.
**Date:** 2026-07-15

---

Verdict:
approve-with-amendments

Critical issues:
None.

Evidence or classification discrepancies:
1. `PT-A1-25` ("Split Edit vs Change Grade/Salary row action", `confirmed`, migrated) and the excluded `PT-A1-23` ("Employees.tsx split-action rework: Edit/Change Grade/View Contracts", `tentative`) both describe the same Sprint 17 Track B3 delivery increment and rest on the identical evidence base — `docs/test-reports/2026-05-27-sprint-17-full.md`'s B3 section, where every row-action/SlideOver check is code-level PASS and the only gap is "Browser testing: NOT executed." The discovery document gives these two overlapping descriptions of what looks like the same underlying feature two different confidence labels without reconciling the overlap. The run record and `PT-A1-25`'s story file disclose that the two items "share the same browser UAT BLOCKED limitation," but neither addresses the more fundamental question of whether `PT-A1-23` and `PT-A1-25` are actually duplicate/overlapping discovery-document entries for one delivery, which would mean the batch is simultaneously treating the same delivered work as both excluded (`PT-A1-23`) and confirmed-and-migrated (`PT-A1-25`) under different IDs. This does not on its own invalidate migrating `PT-A1-25` — its own EMP-UX-1-specific acceptance criteria (Edit scoped to name+status, Change-Grade SlideOver with required `change_reason`, row-action visibility rules) are independently code-level verified in the cited test report — but the overlap was not surfaced as its own discrepancy and should be.
2. The other four disclosed evidence-caveat items were independently re-verified and found accurately disclosed, with no further discrepancy: `PT-A1-09` (WC-6 add-flow genuinely recorded as GAP in the 2026-04-21 test report; add-flow code confirmed present via `AddSalaryDefSlideOver`/`POST /salary-definition`, landed the next day per commit `db17ef9`, verified real); `PT-A1-10` (commit `0a2702d`, dated 2026-07-05, verified real and matches the described one-way Withdraw supersession); `PT-A1-28`/`PT-A1-38`/`PT-A1-39` (commits `8a9d548` and `98f2100` verified real and consistent with the claimed changes; no dedicated test report exists for any of the three, exactly as disclosed); `PT-A1-41`/`PT-A1-42` (the cited Sprint 16 test report's own overall verdict line reads verbatim "PASS (code-level); runtime deferred to staging," confirmed by direct inspection).

Hierarchy, name-display, or registry defects:
None. `python3 docs/product/validate_registry.py` passes (32 rows, 0 errors). All 19 new `STORY-REGISTRY.md` rows resolve to `FEAT-3`/`FEAT-4`/`FEAT-5`, which resolve to `CAP-3`, which resolves to `OUT-3`; every displayed `feature_name`/`capability_name`/`outcome_name` was spot-checked against its parent's authoritative `name` column and matches exactly, including the two pre-existing Phase 4A rows (`PT-A4-31`/`PT-A4-32`, `CAP-1`/`CAP-2`, `FEAT-1`/`FEAT-2`) under the amended schema. `FEATURES.md` story_count values (9/8/2) sum correctly to the 19 migrated rows. All 19 story filenames begin with their full exact story ID; no ambiguous prefix found. `validate_registry.py`'s duplicate-ID and ambiguous-prefix checks (`check_duplicate_ids`, the two-way `stem_matches_story_id` scan) were read and, by direct reasoning through the code, would correctly reject a missing/mismatched display name (lines 111–119, 130–138, 153–161), a duplicate ID (lines 69–78), and an ambiguous story-file prefix match (lines 168–188) — this was confirmed by code reading only, not by an actual injected fault, per the instruction not to edit the file to test this.

Guardrail gaps:
None found. `git status --short` confirms all changes are confined to `docs/product/` and `docs/programmes/product-traceability/`, aside from four pre-existing, unrelated working-tree items (`docs/ROADMAP.md` modified, `docs/test-harness-checklist.md` deleted, `docs/test-reports/test-harness/` untracked, `docs/ux-design-brief/gate-6/ia-timesheet-discoverability.md` untracked) that the run record correctly identifies as out of scope and untouched by this run. `git diff docs/ROADMAP.md` confirms the diff is a Phase-numbering correction (Phase 1a/1b/1c relabelling) with no relationship to this migration. None of the 19 migrated IDs match the mandatory exclusions (`PT-A1-23`, `PT-A1-24`, `PT-A1-45` are absent from `STORY-REGISTRY.md`, confirmed by direct inspection); all 19 carry the `confirmed` label in the discovery document's own Section 3.1 table, verified ID-by-ID. Programme-control files (`decisions.md` D-016, `PHASES.md`, `state.md`, `phase-inputs.yaml`) consistently state Phase 4 as a whole remains unauthorised and the next permitted action is human review. Two spot-checked confirmed-item counts for capability areas other than A1+A2 (A5 = 2, A6 = 3) both match the discovery document exactly; A4's cited count of 13 also matches.

Required amendments:
1. Add an explicit disclosure to `PT-A1-25`'s story file (and/or `exceptions.md`'s Phase 4B section) stating that its title and evidence base substantially overlap with the excluded `PT-A1-23`, and recording a reasoned judgement on whether they are the same delivered item viewed at two granularities or genuinely distinct scopes — rather than only noting the shared "browser UAT BLOCKED" caveat. This is a disclosure gap, not a basis for excluding `PT-A1-25` outright, since its specific EMP-UX-1 acceptance criteria are independently code-level verified.

Human decisions still required:
- Whether the five evidence-caveat items should get a dedicated follow-up re-verification pass (already flagged as an outstanding item in the run record) — unchanged by this review.
- Whether `PT-A1-23`/`PT-A1-24`/`PT-A1-45` should be revisited in a future batch once evidence gaps close — unchanged by this review.
- Explicit authorisation of any further Phase 4 migration batch scope — not decided by this review; this batch's completion does not auto-authorise it.

Re-review: not required for the required amendment above — it is a disclosure-text addition to an already-migrated story file, not a change to any registry row, hierarchy relationship, or migrated-item set, and does not affect the validator result or reconciliation proof. The executor may apply it and proceed to commit without a further critic pass, per the "purely mechanical correction" waiver.
