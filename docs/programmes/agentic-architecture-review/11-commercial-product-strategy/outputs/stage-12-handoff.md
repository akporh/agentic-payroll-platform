# Stage 11 → Stage 12 Handoff (Target Direction)

Strategy-synthesis inputs from the commercial/product-strategy diagnostic. Stage 12 synthesises the target direction; nothing here decides it.

## 1. The value map, compressed

Full map: `commercial-value-map.md`. The shape Stage 12 should synthesise from:

- **Three sellable stories**, all gate-disciplined: compliance (C12+C11, B5-measured), onboarding (C14+C13, B1/B2/B3-measured), input quality (C7, calibration-report-backed, structurally lagged by shadow exit).
- **One cross-capability differentiator**: the assurance posture itself — four standing-artifact-backed claims (`positioning-and-claims.md` §2) that are properties of the platform, not of any capability.
- **Deterministic-first value reality**: the two capabilities that carry claimable value soonest (C12, C14) are deterministic; every AI differentiator sits behind unbuilt foundations. Commercial value order *matches* technical readiness order (DEC-11-03) — no build-vs-value tension exists for Stage 12 to trade off.
- **Non-value rows are binding**: C4/C8/C9/C15 contribute nothing to the target narrative until their own conditions change (D-02/D-03 conditions preserved).

## 2. Positioning inputs

`positioning-and-claims.md` in full — headline for synthesis: **"a payroll platform with an assistant" (Principle 8), led by the queue/approval/evidence surfaces where auditable AI is visible**; claims paced by gate evidence and baselines, with the overclaim table (§3) as a hard boundary and the honesty-asymmetry framing (§4) as the narrative angle. No claim has demand-side validation (F-11-02) — Stage 12's direction statement should distinguish "provable" from "proven to sell."

## 3. Boundary classifications the direction must respect

From `product-scope-boundaries.md`:

- **DQ-005 is closed** (concurrence): CORRECTION UI = context-launched CTA with/after C12. No open surface-scope questions remain from Stages 09/11.
- **Multi-operator**: later-increment with named triggers; may be *promoted to prerequisite* by DQ-007's resolution — direction statements about team workflows should stay conditional until DQ-007 resolves.
- **Multi-tenant SaaS**: **the direction fork Stage 12 must state explicitly** — "single-bureau excellence, SaaS-ready posture" is executor-safe and evidence-supported; "active SaaS trajectory" requires the human decision bundle (product + RR-1 re-open + scope; F-11-01: intent documented, everything else absent). Do not blur the fork; RR-1 trigger (c) was checked and not fired this stage (DEC-11-04).
- **C15 / operational reporting**: later-increments; the reporting area re-enters assessment only after audit-coverage + exception substrate exist.

## 4. Constraints inherited whole

- `sequencing-economics.md` — the full constraint set (orderings O1–O9, windows W1–W6, one-off costs, capped operating cadence). Stage 12's direction must be *achievable inside it*; Stage 13 sequences within it.
- Measurement-framework prohibitions apply to any KPI the direction proposes (no usage-volume success metrics; precision over volume; dry-run ≠ validated).
- The single-operator cadence ceiling (one monthly + one quarterly session) bounds how many live AI capabilities the direction can assume operating concurrently.

## 5. Evidence gaps the direction should not paper over

- **EG-004**: next-onboarding timing unknown — determines whether the onboarding story's baselines (B1/B2) are capturable (unrecoverable window W2).
- **EG-005**: no demand/willingness-to-pay evidence for any capability or for SaaS — direction language should be capability-led ("what we can credibly offer"), not demand-led ("what the market asked for"), until Michael supplies client-side facts (type-5 statements).
