# System Reality Report
**Generated:** 2026-04-07  
**Method:** Active reconciliation of analysis artefacts (0.0–0.6) against codebase.  
**Scope:** Backend only (DB schema, migrations, Python services, API routes). Frontend behaviour is noted where inferable from backend surfaces.

---

## Part 0 — Corrections to Analysis Artefacts

The following findings in the prior analysis documents are **factually wrong**. All other findings are confirmed.

| # | Artefact | Incorrect Claim | Actual State |
|---|---|---|---|
| C1 | 0.0-highlights, 0.4-gap-outcomes G3 | "health_insurance_flat / development_levy_flat / life_insurance_rule — no calculation logic" | All three handlers have full logic in `sequential_executor.py:397–420`. Context keys are populated in `payroll.py:467–469` and `payroll_retry_service.py:311–313`. The gap is not missing logic — it is missing data in `statutory_rule.rules_jsonb` (see Finding F1 below). |
| C2 | 0.0-highlights | "GET /{wid}/platform-components and GET /{wid}/component-overrides — no matching routes found" | Both routes exist in `backend/api/routes/workspace.py:527` and `:566`. They are wired and functional. |
| C3 | 0.0-highlights | "GET /{wid}/payroll/runs/{runId}/timeline — no backend route found" | Route exists at `backend/api/routes/payroll.py:866`. It calls `get_trace_steps(run_id)` from `execution_trace_repo.py`. |
| C4 | 0.4-gap-outcomes G5 | "Second reconcile call causes DB IntegrityError → 500 (not 409)" | Fixed. `reconciliation_repo.py:72–76` catches `IntegrityError` and re-raises as `ValueError("already exists...")`. The scoped route at `payroll.py:861` catches `ValueError` and returns 409. G5 is resolved. |

---

## Part 1 — Business Rules Reconciliation

Legend: ✅ Confirmed · ⚠️ Partial · ❌ Missing · 🔴 Orphaned

### 1.1 Workspace & Tenancy

| Rule | Description | DB | Code | Status | Notes |
|---|---|---|---|---|---|
| W1 | Every workspace belongs to an account | `workspace.account_id FK NOT NULL` | — | ✅ Confirmed | |
| W2 | Workspace name required | `NOT NULL` | — | ✅ Confirmed | No uniqueness constraint on name — two workspaces can share a name. |
| W3 | Country code required | `NOT NULL` | — | ✅ Confirmed | |
| W4 | Base currency defaults NGN | `DEFAULT 'NGN'` | — | ✅ Confirmed | |
| W5 | Status defaults DRAFT | `DEFAULT 'DRAFT'` | — | ✅ Confirmed | |
| W6 | Payroll run blocked if workspace not LIVE | Trigger `enforce_workspace_live_before_payroll` | — | ✅ Confirmed | |
| W7 | Workspace state machine | — | `workspace_state_machine.py` | ✅ Confirmed | Python-layer only; no DB-level state transition trigger. |
| W8 | country_code implies which statutory rules apply | **No FK; no validation** | No check in `POST /workspace` | ❌ Missing | A workspace created with `country_code='XX'` will fail silently on first payroll run. Confirmed gap G8. |

### 1.2 Employee & Contract

| Rule | Description | DB | Code | Status | Notes |
|---|---|---|---|---|---|
| E1–E4 | Workspace FK, name, employee number uniqueness, status default | Constraints confirmed in migrations | — | ✅ Confirmed | |
| E5 | At most one active contract per employee | `uq_employee_active_contract WHERE end_date IS NULL` | — | ✅ Confirmed | |
| E6–E9 | Contract FK, workspace FK, start date, end_date null = active | Constraints confirmed | — | ✅ Confirmed | |
| E10 | grade_id and designation_id nullable on contract | Nullable FKs | — | 🔴 Orphaned | Populated during onboarding. Never read in any calculation path (`executor.py`, `sequential_executor.py`, `rule_evaluator.py`). |
| E11 | Salary definition must be effective for a pay period | Columns `effective_from`, `effective_to` exist | **Never queried** | ❌ Missing | `payroll.py` JOIN does not filter by `effective_from`/`effective_to`. |
| E12 | Only ACTIVE employees included in payroll | — | `WHERE status='ACTIVE'` in `payroll.py:111` | ✅ Confirmed | |

### 1.3 Salary Definition

| Rule | Description | DB | Code | Status | Notes |
|---|---|---|---|---|---|
| SD1–SD3 | Workspace FK, name uniqueness, components_jsonb type check | Constraints confirmed | — | ✅ Confirmed | |
| SD4 | Cannot be modified after a PAID run | Trigger `prevent_salary_definition_change_if_used` | — | ✅ Confirmed | |
| SD5 | Must contain BASIC, HOUSING, TRANSPORT | — | `hard_validator.py` (onboarding only) | ⚠️ Partial | Enforced only during onboarding JSON upload. Direct API insert to `salary_definition` bypasses this check. No DB-level constraint. |
| SD6 | components_jsonb keys must correspond to active component_metadata | None | None | ❌ Missing | Unknown component codes are silently ignored in calculation. |
| SD7 | effective_from / effective_to constrains validity | Columns exist | **Never read at run time** | 🔴 Orphaned | Stored in DB; never queried during execution. `payroll.py` does not filter by these dates. |

### 1.4 Pay Cycle

| Rule | Description | DB | Code | Status | Notes |
|---|---|---|---|---|---|
| PC1 | Workspace FK | `NOT NULL` | — | ✅ Confirmed | |
| PC2 | frequency, run_day, cutoff_day, payment_day stored | Present | — | ✅ Confirmed | |
| PC3 | definition_json extension field | Nullable JSONB | `payroll.py:207`: loaded, passed as `pay_cycle_definition` to persister | ⚠️ Partial | Loaded and persisted in `payroll_run`. Never read again after persistence. Scheduler does not read it. |
| PC4 | At most one active pay cycle | `is_active DEFAULT TRUE` | No uniqueness guard | ❌ Missing | Multiple active pay cycles possible per workspace. `LIMIT 1` in query masks this. |
| PC5 | Pay cycle defines when runs are triggered | Informational only | No scheduler reads this | ❌ Missing | |

**Orphaned fields:** `run_day`, `cutoff_day`, `payment_day` — stored, never read.

### 1.5 Statutory Rules & Tax Bands

| Rule | Description | DB | Code | Status | Notes |
|---|---|---|---|---|---|
| SR1–SR3 | country_code, effective_from, version required | NOT NULL | — | ✅ Confirmed | |
| SR4 | Most recent rule before period_end governs a run | — | `payroll.py:142–150` temporal query | ✅ Confirmed | |
| SR5 | Tax bands must cover sequential ranges | None | None | ❌ Missing | Gaps in bands produce silent zero-tax for income in the gap. |
| SR6 | Tax bands ordered by lower_limit ascending | None | `payroll.py:186–196` uses `ORDER BY lower_limit` on load | ✅ Confirmed | Query enforces order. |
| SR7 | Band rate non-negative | No CHECK | None | ❌ Missing | |
| SR8 | Pension rates from `rules_jsonb.pension` | — | `payroll.py:162–169` | ✅ Confirmed | Hard-required; raises 500 if missing. Migration `c0d1e2f3a4b5` seeds rates. |
| SR9 | NHF rate from `rules_jsonb.nhf` | — | `payroll.py:176`: reads `nhf.rate` | ⚠️ Partial — **key mismatch** | Catalogue shows DB stores `nhf.employee_rate`. Code reads `nhf.rate`. Always falls back to hardcoded `0.025`. NHF rate in `statutory_rule.rules_jsonb` is effectively unreadable. Financially harmless (0.025 is correct), but the statutory rule does not control NHF rate. |
| SR10 | Rent relief config from `rules_jsonb.reliefs.rent_relief` | — | `payroll.py:173` loads config; `sequential_executor.py` uses it | ⚠️ Partial | Rate stored as `"TBD"` string in known DB state. If accessed in calculation it will raise a `Decimal` conversion error at runtime. |
| SR11 | rules_jsonb keys must match handler expectations | No schema validation | None | ❌ Missing | Key mismatches (SR9, SR10) are silent. |
| SR12 | Two rules with same effective_from/version tie-break | Non-deterministic | No uniqueness constraint | ❌ Missing | Confirmed gap G7. |
| — | `statutory_rule.state` (DRAFT/LIVE) | Column exists | **Never checked** | 🔴 Orphaned | Rule resolution does not filter by `state`. A DRAFT rule is applied identically to a LIVE rule. |

### 1.6 Payroll Rules (Legacy)

| Rule | Status | Notes |
|---|---|---|
| PR1–PR3 | ✅ Confirmed | DB constraints enforced. |
| PR4 (Superseded by rule_sets) | ⚠️ Partial | `payroll_rule` is still read to derive valid `input_codes` in `payroll_input.py:list_input_codes`. Execution uses `rule_set_item`. Input code discovery and rate resolution live in different tables. |
| PR5 | ✅ Confirmed | `rule_evaluator.py` dispatches on `calculation_method`. |

### 1.7 Rule Sets (Temporal Versioning)

| Rule | Status | Notes |
|---|---|---|
| RS1–RS3 | ✅ Confirmed | DB constraints enforced. |
| RS4 | ✅ Confirmed | `payroll.py:235–242` temporal query. |
| RS5 | ❌ Missing | No schema validation on `rule_definition_json` format. |
| RS6 | ❌ Missing | Multiple rule sets can share the same `effective_from` per workspace; tie-breaking uses `created_at DESC` (non-deterministic if inserted in same transaction). |

### 1.8 Component Metadata

| Rule | Status | Notes |
|---|---|---|
| CM1 | ✅ Confirmed | `sequential_executor.py:514–519` sorts by `execution_priority ASC`. |
| CM2 | ✅ Confirmed | Unrecognised method raises `KeyError` (surfaced as error). |
| CM3 | ✅ Confirmed | `is_active=FALSE` components filtered before sort. Workspace can disable via `client_component_metadata.overrides_json.is_active=false`. |
| CM4 | ✅ Confirmed | Deep-merge in `payroll.py:339–355`. Client wins on conflict. |
| CM5 | ❌ Missing | No schema validation on `metadata_json`. |

### 1.9 Payroll Run

| Rule | Status | Notes |
|---|---|---|
| RUN1–RUN5 | ✅ Confirmed | All DB-level constraints and triggers verified in migrations. |
| RUN6 | ✅ Confirmed | Trigger `validate_payroll_status_transition` + Python state machine. |
| RUN7 | ✅ Confirmed | Trigger `prevent_paid_payroll_run_update`. |
| RUN8 | ✅ Confirmed | Trigger `prevent_run_snapshot_update`. |
| RUN9 | ✅ Confirmed | `retry_strategy DEFAULT 'PER_EMPLOYEE'`. |
| RUN10 | ✅ Confirmed | `run_type DEFAULT 'REGULAR'`. |
| RUN11 | ❌ Missing | `total_gross_pay` / `total_net_pay` not recalculated after retry. No DB constraint enforces equality with SUM of results. G1 confirmed. |
| RUN12 | ✅ Confirmed | `statutory_effective_date` stored on `payroll_run`; used by retry service for temporal lookup. |
| RUN13 | ✅ Confirmed | Trigger `enforce_payroll_readiness` calls `validate_payroll_readiness()`. |
| — | `payroll_run.pay_date` | 🔴 Orphaned | Stored from UI; never read in any calculation. |
| — | `payroll_run.rule_set_id` | ⚠️ Partial | NULL for default runs; set for override runs. Not referenced inside `sequential_executor.py` — only used at data-load time to scope which `rule_set_item` rows are loaded. |

### 1.10 Payroll Result

| Rule | Status | Notes |
|---|---|---|
| RES1–RES3, RES6 | ✅ Confirmed | DB constraints enforced. |
| RES4 | ✅ Confirmed | Trigger `prevent_snapshot_update`. |
| RES5 | ✅ Confirmed | Trigger `prevent_payroll_result_mutation`. |
| RES7 | ❌ Missing | Negative net pay by design allowed (overpayment recovery). No floor or flag. G9 confirmed. |
| RES8 | ⚠️ Partial | `status` is a varchar, not an ENUM. No DB-level constraint prevents arbitrary strings. Application writes `SUCCESS` / `FAILED`. |
| RES9 | ⚠️ Partial | `component_trace_jsonb` populated by sequential executor path; NULL for legacy executor path (`executor.py:115`). No structured reader exposed. |

### 1.11 Payroll Input

| Rule | Status | Notes |
|---|---|---|
| INP1–INP3 | ✅ Confirmed | DB constraints enforced. |
| INP4–INP7 | ✅ Confirmed | Application-level claiming logic in `payroll_input_repo.py`. |
| INP8 | ✅ Confirmed | `_load_workspace_input_codes()` validates at submission. |
| INP9 | ❌ Missing | No uniqueness constraint on `(workspace_id, employee_id, input_code, reference_date)`. Duplicate bulk submission doubles quantities. G11 confirmed. |
| INP10 | ❌ Missing | No quantity validation. Negative quantity accepted. |
| `source` | 🔴 Orphaned | Written at creation; never read in any downstream process. |
| `input_json` | 🔴 Orphaned | No known reads in execution code. |

### 1.12 Payroll Reconciliation

| Rule | Status | Notes |
|---|---|---|
| RC1 | ✅ Confirmed | Unique constraint on `payroll_run_id`. |
| RC2 | ⚠️ Partial | `expected_total` reflects `payroll_run.total_net_pay` at time of reconciliation — stale after retry. G1 causes RC2 to be unreliable. |
| RC3 | ✅ Confirmed | Comparison in `reconciliation_service.py:62`. |
| RC4 | ❌ Missing | No run state gate. Can reconcile a DRAFT or CALCULATING run. G4 confirmed. |
| RC5 | ❌ Missing | MISMATCH is recorded; no corrective workflow is triggered. |
| RC6 (duplicate = 500) | ✅ **Resolved** | `reconciliation_repo.py:72–76` catches `IntegrityError`, re-raises `ValueError`. Scoped route converts to 409. **G5 is fixed.** |

### 1.13 Audit Log & Event Store

| Rule | Status | Notes |
|---|---|---|
| AL1 | ⚠️ Partial | Written for: DRAFT→CALCULATING, CALCULATING→CALCULATED/PARTIAL, CALCULATED→APPROVED, APPROVED→LOCKED, LOCKED→PAID. **Not written for retry operations.** G2 confirmed. |
| AL2 | ⚠️ Partial | No UPDATE/DELETE trigger; immutability is by convention only. |
| AL3 | ⚠️ Partial | `performed_by` hardcoded to `"admin@internal"` in `payroll.py:489` and `payroll_approval_service.py:95,165`. Only `mark_payroll_run_paid` accepts a real `actor_id` parameter. |
| EV1 | ⚠️ Partial | Same gaps as AL1. |
| EV2 | ❌ Missing | No read API for `event_store` or `audit_log`. Tables are write-only from the application's perspective. |

### 1.14 Cross-Table Rules

| Rule | Status | Notes |
|---|---|---|
| X1 | ❌ Missing | No DB constraint that `total_net_pay = SUM(payroll_result.net_pay)`. G1 makes this structurally impossible post-retry. |
| X2 | ✅ Confirmed | `payroll_result.payroll_run_id FK NOT NULL`. |
| X3 | ❌ Missing | No cross-workspace FK guard. WHERE clauses are the only protection. |
| X4 | ✅ Confirmed | Resolved at query time by `country_code` join. |
| X5 | ⚠️ Partial | WHERE clause in repo; no DB-level guard. |
| X6 | ❌ Missing | `payroll_run.total_net_pay` used for reconciliation is stale after retry. G1 + G4 compound. |

---

## Part 2 — Calculation Engine Reality

### 2.1 Component Handler Status (all confirmed via `sequential_executor.py` + `payroll.py`)

| Priority | Component | Handler | Context Source | Status | Finding |
|---|---|---|---|---|---|
| 10 | BASIC | `salary_component` | `components_jsonb` | ✅ Confirmed | |
| 20 | HOUSING | `salary_component` | `components_jsonb` | ✅ Confirmed | |
| 30 | TRANSPORT | `salary_component` | `components_jsonb` | ✅ Confirmed | |
| 40 | CONSOLIDATED_ALLOWANCE | `salary_component` | `components_jsonb` | ✅ Confirmed | |
| 100 | GROSS_PAY | `sum_earnings` | Accumulated results | ✅ Confirmed | Sums all `salary_component` outputs. |
| 200 | PENSION_EMPLOYEE | `pension_rule` | `pension_employee_rate` from `rules_jsonb.pension.employee_rate` | ✅ Confirmed | Base = BASIC+HOUSING+TRANSPORT (configurable). Employee + employer side-effect calculated. |
| 250 | RENT_RELIEF | `rent_relief` | `rent_relief_cfg` from `rules_jsonb.reliefs.rent_relief` | ⚠️ Partial | Skipped when `ANNUAL_RENT_PAID` input absent. Rate stored as `"TBD"` in known DB state — Decimal conversion error at runtime if any employee has ANNUAL_RENT_PAID input. |
| 300 | TAXABLE_INCOME | `taxable_income` | `deduct_components` list | ✅ Confirmed | Defaults: GROSS_PAY − PENSION_EMPLOYEE − RENT_RELIEF. Configurable via `metadata_json.deduct_components`. |
| 400 | PAYE | `paye_rule` | `tax_bands` | ✅ Confirmed | Applied to TAXABLE_INCOME. Raises ValueError if `tax_bands` empty. |
| 410 | NHF_CONTRIBUTION | `nhf_rule` | `nhf_rate` from `rules_jsonb.nhf.rate` | ⚠️ Partial — key mismatch | DB stores `nhf.employee_rate`. Code reads `nhf.rate`. Always uses hardcoded fallback 0.025. Financially correct (0.025 = statutory rate) but statutory_rule cannot override NHF rate. |
| 420 | HEALTH_INSURANCE_EMPLOYEE | `health_insurance_flat` | `health_insurance_employee_amount` from `rules_jsonb.health_insurance.employee_monthly_amount` | ⚠️ Partial — **F1** | Handler logic is implemented. Context key is populated. BUT: DB catalogue shows `rules_jsonb` stores key `employee_amount`, not `employee_monthly_amount`. Read always returns 0 from statutory_rule. Correct value only available if workspace sets `client_component_metadata` override with key `employee_monthly_amount`. |
| 430 | DEVELOPMENT_LEVY | `development_levy_flat` | `development_levy_amount` from `rules_jsonb.development_levy.monthly_amount` | ⚠️ Partial — **F1** | Same pattern as HEALTH_INSURANCE. DB stores `amount`; code reads `monthly_amount`. Returns 0 unless workspace override present. |
| 440 | LIFE_INSURANCE | `life_insurance_rule` | `life_insurance_employer_rate` from `rules_jsonb.life_insurance.employer_rate` | ⚠️ Partial — **F2** | Handler logic implemented. Key `employer_rate` matches DB. Rate resolution works. BUT: `component_class = 'employer_cost'` — NOT `statutory_deduction`. `_handle_net_formula` only sums `statutory_deduction` components, so LIFE_INSURANCE is computed but does **not reduce NET_PAY**. This is correct per PRA 2014 s.9(3) (employer cost, not employee deduction). Not a bug, but not stated clearly in analysis. |
| 500 | NET_PAY | `net_formula` | `_component_map` for class lookup | ✅ Confirmed | Sums all `statutory_deduction` class components; deducts from GROSS_PAY. |

**Finding F1 (New — not in analysis documents):** Health Insurance and Development Levy produce ₦0 not because of missing handler logic (as stated in G3/0.0) but because of a `rules_jsonb` key name mismatch between what is stored (`employee_amount`, `amount`) and what the code reads (`employee_monthly_amount`, `monthly_amount`). The values are only non-zero when configured via workspace-level `client_component_metadata` overrides using the correct keys.

**Finding F2 (New — not in analysis documents):** LIFE_INSURANCE is classified `employer_cost`, not `statutory_deduction`. It is correctly excluded from NET_PAY deduction by design (PRA 2014). Its computed value appears only in `component_trace_jsonb` and `deductions_jsonb` as an employer liability line, not as an employee deduction. This is legally correct but was not stated clearly in analysis artefacts.

### 2.2 Execution Flow — Confirmed Steps

| Step | Description | Implemented | Location |
|---|---|---|---|
| A1 | Load workspace | ✅ | `payroll.py:79–88` |
| A2 | Idempotency check | ✅ | `payroll.py:91–108` |
| A3 | Load active employees | ✅ | `payroll.py:110–133` |
| A4 | Temporal statutory rule selection | ✅ | `payroll.py:142–159` |
| A5 | Extract statutory rates | ✅ | `payroll.py:161–179` |
| A6 | Load tax bands | ✅ | `payroll.py:181–196` |
| A7 | Load pay cycle | ✅ | `payroll.py:198–208` (frequency only; `definition_json` loaded but unused) |
| A8 | Build PeriodContext | ✅ | `payroll.py:210–221` |
| A9 | Temporal rule set resolution | ✅ | `payroll.py:223–290` |
| A10 | Load component metadata | ✅ | `payroll.py:292–312` |
| A11 | Load client component overrides | ✅ | `payroll.py:314–355` |
| A12 | Cross-period input prefetch | ✅ | `payroll.py:370–440` |
| A13 | Build rules context snapshot v2 | ✅ | `payroll.py:442–458` |
| B1 | Load unclaimed inputs | ✅ | `payroll.py:361–368` |
| C1 | Pure domain execution | ✅ | `payroll_run_service.py` → `run_executor.py` → `batch_processor.py` |
| D | Single transaction persist | ✅ | `payroll_run_persister.py` |
| E | Post-persist input claiming | ✅ | `payroll.py:532–536` |

### 2.3 Retry Flow Reality

| Step | Described in 0.2 | Implemented | Gap |
|---|---|---|---|
| R1 | SELECT FOR UPDATE | ✅ | |
| R2 | Validate PARTIAL only | ✅ | `payroll_retry_service.py:556–596` |
| R3 | PER_EMPLOYEE branch | ✅ | `payroll_retry_service.py` |
| R3 | FULL_RUN branch | ✅ | `payroll_retry_service.py:421–` |
| R6–R8 | Recount failures, update status, commit | ✅ | |
| — | Recalculate run totals | **Not implemented** | G1: `total_gross_pay`, `total_net_pay`, `total_deduction` not recalculated. |
| — | Write audit entry for retry | **Not implemented** | G2: No `audit_log` or `event_store` row written for retry. |

### 2.4 Approval / Lock / Paid Flow Reality

| Capability | Backend Service | Route | Status |
|---|---|---|---|
| CALCULATED → APPROVED | `payroll_approval_service.py:approve_payroll_run` | `POST /payroll/approve` | ✅ Confirmed |
| APPROVED → LOCKED | `payroll_approval_service.py:lock_payroll_run` | `POST /payroll/lock` | ✅ Confirmed |
| LOCKED → PAID | `payroll_approval_service.py:mark_payroll_run_paid` | `POST /payroll/mark-paid` | ✅ Confirmed |
| Audit written | Yes, for all three transitions | — | ✅ Confirmed |
| `performed_by` identity | Hardcoded `"admin@internal"` for approve/lock | Actor parameter only on mark-paid | ⚠️ Partial |
| Pre-condition guard | None — can approve a PARTIAL that transitioned to CALCULATED | — | ❌ Missing |

### 2.5 Reconciliation Flow Reality

| Step | Status | Notes |
|---|---|---|
| Run state validation | ❌ Missing | Can reconcile DRAFT, CALCULATING, PARTIAL. G4 confirmed. |
| Expected total from `total_net_pay` | ⚠️ Partial | Correct at point-in-time; stale after retry. |
| MATCHED / MISMATCH comparison | ✅ Confirmed | |
| Duplicate → 409 | ✅ **Fixed** | `reconciliation_repo.py` catches IntegrityError → ValueError; route converts to 409. G5 resolved. |
| Correction path | ❌ Missing | No UPDATE path. MISMATCH record cannot be changed without direct DB access. |
| MISMATCH action | ❌ Missing | No workflow triggered. |

---

## Part 3 — API Surface Reality

### 3.1 Backend Logic Not Exposed to Frontend

| Capability | Backend Location | Exposure | Status |
|---|---|---|---|
| Approve / Lock / Mark Paid | `payroll_approval_service.py`, routes `/payroll/approve`, `/lock`, `/mark-paid` | API routes exist; no UI buttons known | Backend-only (API available) |
| Retry failed employees | `payroll_retry_service.py`, route `/payroll/retry` | API route exists; no UI button in PayrollRuns.tsx | Backend-only (API available) |
| Rule set override for a run | `payroll.py:53` accepts `rule_set_id` | Not in RunPayroll.tsx form | Backend-only |
| Export net pay / PAYE / register | `backend/application/exports/` | Export functions exist; **no API endpoints** | ❌ No route wired |
| Audit log / event store read | Tables written; no read routes | No GET endpoints | ❌ No route wired |
| Statutory rule management | `statutory_rule` table | No write or read API | ❌ No route wired |
| Snapshot replay | v2 snapshot in `payroll_run` | No endpoint or replay function | ❌ No route wired |
| Platform components toggle | `workspace.py:527` GET route | Route exists | ✅ Route exists (C2 correction) |
| Component overrides read | `workspace.py:566` GET route | Route exists | ✅ Route exists (C2 correction) |
| Component overrides write (post-onboarding) | No standalone endpoint | Writable only via onboarding commit | ❌ Partial — write-once post-setup |
| Execution timeline | `payroll.py:866`, calls `execution_trace_repo` | Route exists | ✅ Route exists (C3 correction) |
| Reconciliation (workspace-scoped) | `payroll.py:850–863` | Routes exist | ✅ Route exists |

### 3.2 `period_type`, `working_days`, `retry_strategy` Exposure

Backend accepts all three in run payload. `payroll.py:48–54` reads them. RunPayroll.tsx does not send them. Any run triggered from UI uses:
- `period_type`: derived from `pay_cycle.frequency` or inferred from dates
- `working_days`: not set (only required for CUSTOM)
- `retry_strategy`: defaults to `PER_EMPLOYEE`

**Status: ⚠️ Partial** — backend full; UI incomplete.

---

## Part 4 — Fields Populated But Never Used

| Field | Table | Populated By | Never Read By | Classification |
|---|---|---|---|---|
| `effective_from` / `effective_to` | `salary_definition` | Onboarding | Any execution path | 🔴 Orphaned |
| `pay_date` | `payroll_run` | UI / API payload | Any calculation | 🔴 Orphaned |
| `grade_id` | `employee_contract` | Onboarding | Any calculation path | 🔴 Orphaned |
| `designation_id` | `employee_contract` | Onboarding | Any calculation path | 🔴 Orphaned |
| `description` | `grade`, `designation` | Onboarding | Anywhere | 🔴 Orphaned |
| `definition_json` | `pay_cycle` | Onboarding | Execution (loaded, stored in run, never re-read) | 🔴 Orphaned |
| `run_day`, `cutoff_day`, `payment_day` | `pay_cycle` | Onboarding | Scheduler (no scheduler) | 🔴 Orphaned |
| `source` | `payroll_input` | Input API | Any downstream process | 🔴 Orphaned |
| `input_json` | `payroll_input` | Input API | Any downstream process | 🔴 Orphaned |
| `state` | `statutory_rule` | Migration seed | Rule resolution query | 🔴 Orphaned |
| `component_trace_jsonb` | `payroll_result` | Sequential executor | No structured reader; no UI renderer | ⚠️ Stored, no reader |
| `rules_jsonb.nhf.employee_rate` | `statutory_rule` | (Expected by catalogue) | `payroll.py` reads `nhf.rate` instead | 🔴 Orphaned (key mismatch) |
| `rules_jsonb.health_insurance.employee_amount` | `statutory_rule` | (Expected by catalogue) | `payroll.py` reads `employee_monthly_amount` | 🔴 Orphaned (key mismatch) |
| `rules_jsonb.development_levy.amount` | `statutory_rule` | (Expected by catalogue) | `payroll.py` reads `monthly_amount` | 🔴 Orphaned (key mismatch) |

---

## Part 5 — Flows Defined But Not Executed

| Flow / Capability | Where Defined | Why Not Executed |
|---|---|---|
| Export: net pay CSV, PAYE summary, payroll register | `backend/application/exports/*.py` | No API endpoints. CLI/test scripts only. |
| Snapshot replay | v2 snapshot stored in `payroll_run.rules_context_snapshot` | No replay function or endpoint exists. Snapshot used for retry context only. |
| Execution timeline trace | `execution_tracer.py` writes to `execution_trace` table | `ExecutionTracer` context manager exists and has a `NULL_TRACER`. Whether the tracer is injected into real runs depends on caller. Routes do not inject it; scripts do. Trace data is sparse or absent for production runs via API. |
| Statutory rule version management | `statutory_rule` table supports versioning | No write API. No UI. Only updateable via direct DB or migration. |
| Pay cycle enforcement | `pay_cycle` stores `run_day`, `cutoff_day`, `payment_day` | No scheduler reads these. Runs are triggered manually. |
| Audit log / event store read | `audit_log`, `event_store` tables written | No GET endpoint for either table. Data is write-only from application. |
| Reconciliation correction | MISMATCH status is stored | No update path. One INSERT only per run. |

---

## Part 6 — Execution Steps With No Data Backing

| Execution Step | Expected Data | Actual State |
|---|---|---|
| HEALTH_INSURANCE_EMPLOYEE amount from statutory rule | `rules_jsonb.health_insurance.employee_monthly_amount` | Key does not exist in known DB state. Code reads `employee_monthly_amount`; DB stores `employee_amount`. Returns ₦0 unless workspace override is set. |
| DEVELOPMENT_LEVY amount from statutory rule | `rules_jsonb.development_levy.monthly_amount` | Key does not exist in known DB state. Code reads `monthly_amount`; DB stores `amount`. Returns ₦0 unless workspace override is set. |
| NHF rate from statutory rule | `rules_jsonb.nhf.rate` | DB stores `nhf.employee_rate`. Code always falls back to hardcoded `0.025`. Correct value but statutory rule cannot override it. |
| RENT_RELIEF rate calculation | `rules_jsonb.reliefs.rent_relief.rate` | Stored as `"TBD"` string. Decimal conversion will fail at runtime for any employee with an `ANNUAL_RENT_PAID` input. |
| Tax band ordering | `ORDER BY lower_limit` | No constraint enforcing contiguous coverage. Gap in bands produces silent zero-tax for income in the gap. |
| `payroll_run.total_gross_pay`, `total_deduction` post-retry | Current sum of results | Not recalculated. Stale values from initial run persist. |
| Execution trace for API-triggered runs | `execution_trace` rows | `ExecutionTracer` not injected by API routes. Trace is empty or absent for runs triggered via `POST /payroll/run`. |

---

## Part 7 — Summary Classification Table

### Business Rules (35 total from 0.6)

| Category | Total | ✅ Confirmed | ⚠️ Partial | ❌ Missing | 🔴 Orphaned |
|---|---|---|---|---|---|
| Multi-Client Isolation | 5 | 4 | 0 | 1 | 0 |
| Workforce & Compensation | 4 | 2 | 2 | 0 | 0 |
| Payroll Calculation | 7 | 4 | 3 | 0 | 0 |
| Period Management | 3 | 1 | 2 | 0 | 0 |
| Run Lifecycle & Authorisation | 4 | 4 | 0 | 0 | 0 |
| Retry & Recovery | 3 | 1 | 2 | 0 | 0 |
| Reconciliation & Disbursement | 2 | 1 | 1 | 0 | 0 |
| Audit & Compliance | 3 | 0 | 3 | 0 | 0 |
| Data Integrity Guarantees | 4 | 4 | 0 | 0 | 0 |
| **Total** | **35** | **21 (60%)** | **13 (37%)** | **1 (3%)** | — |

> Note: reclassification from 0.6's "Not Implemented" (2) reflects that the disbursement exports exist as code but have no API wiring (Partial, not Missing), and the three handler issues are Partial (logic exists, data missing) not Missing.

### Schema Fields (orphaned)

13 fields across 8 tables confirmed stored but never read in any calculation or API response path.

### API Gaps

| Gap Type | Count |
|---|---|
| Backend logic with no API route | 3 (exports, audit read, statutory rule management) |
| API route with no UI exposure | 5 (approve, lock, mark-paid, retry, run options) |
| UI calls route that doesn't exist | 0 (C2/C3 corrections: routes exist) |

### New Findings (not in any prior artefact)

| # | Finding | Impact |
|---|---|---|
| F1 | `rules_jsonb` key name mismatch for health_insurance and development_levy | Both produce ₦0 unless workspace override configured. Net pay overstated. |
| F2 | LIFE_INSURANCE `component_class = employer_cost` — not deducted from NET_PAY | Correct by statute. Prior G3 classification as "broken" is partially misleading. |
| F3 | `ExecutionTracer` not injected into API-triggered runs | `execution_trace` table is empty for production runs; timeline endpoint returns no data. |
| F4 | `payroll_run.total_gross_pay` and `total_deduction` not returned in list endpoint | `GET /{wid}/payroll/runs` only returns `total_net_pay`. Other aggregate fields stored but not exposed. |
| F5 | `performed_by` hardcoded in approve and lock transitions | Audit trail records `"admin@internal"` as actor for all approve and lock events. Only mark-paid accepts a real actor. |

---

## Part 8 — Bureau Go/No-Go Decisions (Updated)

| # | Decision Required | Prior Status | Reality |
|---|---|---|---|
| D1 | Health insurance, development levy scope | "Produce ₦0 silently" | Logic implemented. ₦0 is due to `rules_jsonb` key mismatch (F1). Fix: seed correct keys in statutory_rule or confirm workspace-level override as the only configuration path. |
| D2 | Reconciliation state gate | No gate | Confirmed absent. Reconcile after LOCKED/PAID requires code change. |
| D3 | Approve/Lock/Paid UI | API-only | Routes exist. UI buttons required. |
| D4 | Disbursement file exports | Not wired | Export functions exist; need API endpoints + post-LOCKED trigger. |
| D5 | Audit log read access | No read API | Routes need to be built. |
| D6 | Payslip generation | Not implemented | No payslip renderer. |
| D7 | Statutory rule version management | Requires DB access | No API or UI. |
| D8 | Silent rate fallback flagged | Invisible | `rule_trace.resolution_source` exists; fallback not distinguished. |
| D9 | Run totals after retry | Stale | Retry does not recalculate totals. |
| D10 *(new)* | NHF rate controllable via statutory rule | Not working | Key mismatch. Fix: change code to read `nhf.employee_rate` or reseed DB with `nhf.rate`. |
| D11 *(new)* | Rent relief rate before go-live | Will crash | Rate stored as `"TBD"`. Must be set to a numeric string before any employee submits ANNUAL_RENT_PAID input. |
| D12 *(new)* | Execution trace for audit | Empty in production | Tracer must be injected into API run path or `execution_trace` table serves no purpose. |
