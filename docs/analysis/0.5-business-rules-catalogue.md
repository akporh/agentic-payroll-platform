# Database Business Rules Catalogue

Generated: 2026-04-07. Extracted from schema, migrations, triggers, constraints, and JSONB structures.

---

## WORKSPACE & TENANCY

| # | Rule | Source | Type | Enforced At |
|---|------|--------|------|-------------|
| W1 | Every workspace must belong to an account | `workspace.account_id FK NOT NULL` | Validation | DB level |
| W2 | Workspace name is required | `workspace.name NOT NULL` | Validation | DB level |
| W3 | Workspace country code is required | `workspace.country_code NOT NULL` | Validation | DB level |
| W4 | Base currency defaults to NGN | `workspace.base_currency DEFAULT 'NGN'` | Calculation | DB level |
| W5 | Workspace status defaults to DRAFT | `workspace.status DEFAULT 'DRAFT'` | State constraint | DB level |
| W6 | Payroll run blocked if workspace is not LIVE | Trigger `enforce_workspace_live_before_payroll` — BEFORE INSERT on `payroll_run` | State constraint | DB level |
| W7 | Workspace status progresses through defined states only | `workspace_state_machine.py` ALLOWED_TRANSITIONS | State constraint | Application level only |
| W8 | Workspace country_code implies which statutory rules apply | No FK; link is `statutory_rule.country_code = workspace.country_code` at query time | Temporal rule | **Not enforced** |

**Implied but not enforced:** W8 — no FK from workspace to statutory_rule; if no statutory_rule row exists for a country_code, payroll fails at runtime with no pre-creation warning. No uniqueness constraint on `workspace.name`.

---

## EMPLOYEE & CONTRACT

| # | Rule | Source | Type | Enforced At |
|---|------|--------|------|-------------|
| E1 | Employee must belong to a workspace | `employee.workspace_id FK NOT NULL` | Validation | DB level |
| E2 | Employee full name is required | `employee.full_name NOT NULL` | Validation | DB level |
| E3 | Employee number must be unique within a workspace | `uq_employee_number_per_workspace ON employee(workspace_id, employee_number)` | Validation | DB level |
| E4 | Employee status defaults to ACTIVE | `employee.status DEFAULT 'ACTIVE'` | State constraint | DB level |
| E5 | At most one active contract per employee | `uq_employee_active_contract ON employee_contract(employee_id) WHERE end_date IS NULL` | Validation | DB level |
| E6 | Contract must reference a valid salary definition | `employee_contract.salary_definition_id FK NOT NULL` | Validation | DB level |
| E7 | Contract must belong to a workspace | `employee_contract.workspace_id FK NOT NULL` | Validation | DB level |
| E8 | Contract start date is required | `employee_contract.start_date NOT NULL` | Validation | DB level |
| E9 | end_date IS NULL means the contract is active | Partial unique index `WHERE end_date IS NULL` | State constraint | DB level (partial) |
| E10 | Grade and designation on a contract are optional | `grade_id NULLABLE`, `designation_id NULLABLE` | Validation | DB level |
| E11 | Salary definition must be effective for a payroll period | `salary_definition.effective_from`, `effective_to` columns exist | Temporal rule | **Not enforced** |
| E12 | Only ACTIVE employees included in payroll | Application `WHERE status='ACTIVE'` filter | State constraint | Application level only |

**Orphaned fields:** `employee_contract.grade_id`, `employee_contract.designation_id` — FKs populated during onboarding; not read during payroll execution.

---

## SALARY DEFINITION

| # | Rule | Source | Type | Enforced At |
|---|------|--------|------|-------------|
| SD1 | Must belong to a workspace | `salary_definition.workspace_id FK NOT NULL` | Validation | DB level |
| SD2 | Name unique within a workspace | `uq_salary_definition_name_per_workspace ON salary_definition(workspace_id, name)` | Validation | DB level |
| SD3 | components_jsonb must be a JSON object | `CHECK (jsonb_typeof(components_jsonb) = 'object')` | Validation | DB level |
| SD4 | Cannot be modified if used in a PAID run | Trigger `prevent_salary_definition_change_if_used` — joins through employee_contract → payroll_result → payroll_run WHERE status='PAID' | State constraint | DB level |
| SD5 | Must contain BASIC, HOUSING, TRANSPORT components | `hard_validator.py` — onboarding validation only | Validation | Application level only |
| SD6 | components_jsonb keys must correspond to active component_metadata | No constraint | Validation | **Not enforced** |
| SD7 | effective_from / effective_to constrains when a definition is valid | Columns exist; not queried during execution | Temporal rule | **Not enforced** |

**Orphaned fields:** `salary_definition.effective_from`, `salary_definition.effective_to` — stored; never checked at run time.

---

## GRADE & DESIGNATION

| # | Rule | Source | Type | Enforced At |
|---|------|--------|------|-------------|
| GD1 | Grade code unique within a workspace | `uq_grade_code_per_workspace ON grade(workspace_id, grade_code)` | Validation | DB level |
| GD2 | Designation code unique within a workspace | `uq_designation_code_per_workspace` | Validation | DB level |
| GD3 | Grade and designation not used in payroll calculations | No reads in any calculation handler | State constraint | Not enforced (informational only) |

**Orphaned fields:** `grade.description`, `designation.description` — stored; never read in any execution path.

---

## PAY CYCLE

| # | Rule | Source | Type | Enforced At |
|---|------|--------|------|-------------|
| PC1 | Must belong to a workspace | `pay_cycle.workspace_id FK NOT NULL` | Validation | DB level |
| PC2 | frequency, run_day, cutoff_day, payment_day stored | Columns present | Validation | DB level |
| PC3 | definition_json is an extension field | Nullable JSONB; stored but never read | Validation | **Not enforced** |
| PC4 | At most one active pay cycle per workspace | `is_active DEFAULT TRUE`; no unique constraint on `(workspace_id) WHERE is_active=TRUE` | Validation | **Not enforced** |
| PC5 | Pay cycle defines when runs should be triggered | Informational only; no scheduler reads this | Temporal rule | **Not enforced** |

**Orphaned fields:** `pay_cycle.definition_json`, `run_day`, `cutoff_day`, `payment_day` — stored; never read during execution.

---

## STATUTORY RULES & TAX BANDS

| # | Rule | Source | Type | Enforced At |
|---|------|--------|------|-------------|
| SR1 | country_code required | `statutory_rule.country_code NOT NULL` | Validation | DB level |
| SR2 | effective_from required | `statutory_rule.effective_from NOT NULL` | Temporal rule | DB level |
| SR3 | version required | `statutory_rule.version NOT NULL` | Temporal rule | DB level |
| SR4 | Most recent rule before period_end governs a run | `SELECT WHERE effective_from <= period_end ORDER BY effective_from DESC, version DESC LIMIT 1` | Temporal rule | Application level only |
| SR5 | Tax bands must cover sequential ranges without gaps | Implied by progressive PAYE; no constraint enforces contiguity | Calculation | **Not enforced** |
| SR6 | Tax bands must be ordered by lower_limit ascending | `paye.py` iterates bands; no ORDER BY in query | Calculation | **Not enforced** |
| SR7 | Tax band rate must be non-negative | No CHECK constraint | Validation | **Not enforced** |
| SR8 | Pension employee rate from `rules_jsonb.pension.employee_rate` | Parsed from JSONB | Calculation | Application level only |
| SR9 | NHF rate from `rules_jsonb.nhf.employee_rate`; default 0.025 | Parsed from JSONB | Calculation | Application level only |
| SR10 | Rent relief config from `rules_jsonb.reliefs.rent_relief` | Parsed from JSONB; `rate` key currently stored as `"TBD"` — runtime error | Calculation | Application level only |
| SR11 | `rules_jsonb` keys must match handler expectations | No schema validation | Validation | **Not enforced** |
| SR12 | Two rules with same effective_from/version tie-break non-deterministically | ORDER BY result depends on insertion order | Temporal rule | **Not enforced** |

**Known `rules_jsonb` structure:**
```json
{
  "pension": { "employee_rate": "0.08", "employer_rate": "0.10", "statutory_base_components": ["BASIC","HOUSING","TRANSPORT"] },
  "nhf": { "employee_rate": "0.025" },
  "reliefs": { "rent_relief": { "rate": "TBD", "cap": null } },
  "health_insurance": { "employee_amount": 15000 },
  "development_levy": { "amount": 1200 },
  "life_insurance": { "employer_rate": "0.005" }
}
```

---

## PAYROLL RULES (LEGACY)

| # | Rule | Source | Type | Enforced At |
|---|------|--------|------|-------------|
| PR1 | Must belong to a workspace | `payroll_rule.workspace_id FK NOT NULL` | Validation | DB level |
| PR2 | rule_name, rule_type, rule_definition_json required | NOT NULL on all three | Validation | DB level |
| PR3 | rule_definition_json must be a JSON object | `CHECK (jsonb_typeof(rule_definition_json) = 'object')` | Validation | DB level |
| PR4 | Superseded by rule_set / rule_set_item | Legacy table; new code writes to rule_set_item | State constraint | **Not enforced** |
| PR5 | calculation_method in rule_definition_json determines how quantity is processed | Parsed in `rule_evaluator.py`; values: `unit_multiplier`, `daily_rate_deduction`, `fixed_amount` | Calculation | Application level only |

**Split:** `payroll_rule` is still read to derive valid input codes; execution uses `rule_set_item`. Input codes and rates live in different tables.

---

## RULE SETS (TEMPORAL VERSIONING)

| # | Rule | Source | Type | Enforced At |
|---|------|--------|------|-------------|
| RS1 | Must belong to a workspace | `rule_set.workspace_id FK NOT NULL` | Validation | DB level |
| RS2 | effective_from required | `rule_set.effective_from NOT NULL` | Temporal rule | DB level |
| RS3 | Rule set item must belong to a rule set | `rule_set_item.rule_set_id FK NOT NULL` | Validation | DB level |
| RS4 | Latest rule set with effective_from ≤ period_end governs a run | Query in `payroll.py:223–291` | Temporal rule | Application level only |
| RS5 | rule_definition_json must match format expected by rule_evaluator.py | No CHECK constraint | Validation | **Not enforced** |
| RS6 | Multiple rule sets can share same effective_from per workspace | No unique constraint on `(workspace_id, effective_from)` | Temporal rule | **Not enforced** |

---

## COMPONENT METADATA

| # | Rule | Source | Type | Enforced At |
|---|------|--------|------|-------------|
| CM1 | execution_priority determines calculation order | Sorted ASC before execution in `sequential_executor.py:514–519` | Calculation | Application level only |
| CM2 | calculation_method maps to a registered handler | Handler registry in `sequential_executor.py:453–464`; unrecognised method causes KeyError | Calculation | Application level only |
| CM3 | is_active=FALSE components are skipped | Filter applied before sort | State constraint | Application level only |
| CM4 | client_component_metadata.overrides_json overrides platform defaults | Deep merge in application; client wins | Calculation | Application level only |
| CM5 | metadata_json structure must match handler expectations | No CHECK constraint | Validation | **Not enforced** |

**Known `metadata_json` keys:**
```json
{
  "statutory_base_components": ["BASIC","HOUSING","TRANSPORT"],
  "base_component": "BASIC",
  "deduct_components": ["PENSION_EMPLOYEE","RENT_RELIEF"],
  "proration_strategy": "work_days",
  "eligibility": { "require_input": "ANNUAL_RENT_PAID" },
  "legal_role": { "is_pensionable": true, "component_class": "statutory_deduction" },
  "calculations_behaviour": { "monthly_amount": 15000, "proration_strategy": "work_days" }
}
```

---

## PAYROLL RUN

| # | Rule | Source | Type | Enforced At |
|---|------|--------|------|-------------|
| RUN1 | Must belong to a workspace | `payroll_run.workspace_id FK NOT NULL` | Validation | DB level |
| RUN2 | period_start and period_end required | NOT NULL | Validation | DB level |
| RUN3 | One regular run per (workspace, period_start, period_end) | `uq_payroll_run_period ON payroll_run(workspace_id, period_start, period_end)` | Validation | DB level |
| RUN4 | Idempotency key unique per workspace when present | `ux_payroll_run_idempotency ON payroll_run(workspace_id, idempotency_key) WHERE idempotency_key IS NOT NULL` | Validation | DB level |
| RUN5 | Every new run must start in DRAFT | Trigger `enforce_payroll_run_initial_status` — BEFORE INSERT | State constraint | DB level |
| RUN6 | Status can only advance forward | Trigger `validate_payroll_status_transition` — compares ranks; rejects backward | State constraint | DB level |
| RUN7 | PAID run cannot be modified or deleted | Trigger `prevent_paid_payroll_run_update` — BEFORE UPDATE/DELETE when status='PAID' | State constraint | DB level |
| RUN8 | rules_context_snapshot cannot be changed once set | Trigger `prevent_run_snapshot_update` | State constraint | DB level |
| RUN9 | retry_strategy defaults to PER_EMPLOYEE | `payroll_run.retry_strategy DEFAULT 'PER_EMPLOYEE'` | State constraint | DB level |
| RUN10 | run_type defaults to REGULAR | `payroll_run.run_type DEFAULT 'REGULAR'` | State constraint | DB level |
| RUN11 | Totals must equal sum of results | `total_gross_pay`, `total_net_pay` not constrained to match SUM(payroll_result) | Calculation | **Not enforced** |
| RUN12 | statutory_effective_date records date used for statutory rule selection | Stored for retry determinism; not validated | Temporal rule | Application level only |
| RUN13 | Prerequisites must be met before run creation | Trigger `enforce_payroll_readiness` — calls `validate_payroll_readiness()` SQL function | Validation | DB level |

**Implied but not enforced:** RUN11 — totals are not recalculated after retry (G1). The DB has no constraint that `total_net_pay = SUM(payroll_result.net_pay)`.

---

## PAYROLL RESULT

| # | Rule | Source | Type | Enforced At |
|---|------|--------|------|-------------|
| RES1 | One result per employee per run | `uq_payroll_result_employee_run ON payroll_result(payroll_run_id, employee_id)` | Validation | DB level |
| RES2 | gross_components_jsonb must be JSON object | `CHECK (jsonb_typeof(gross_components_jsonb) = 'object')` | Validation | DB level |
| RES3 | deductions_jsonb must be JSON object | `CHECK (jsonb_typeof(deductions_jsonb) = 'object')` | Validation | DB level |
| RES4 | calculations_snapshot_json cannot change after INSERT | Trigger `prevent_snapshot_update` | State constraint | DB level |
| RES5 | Result row cannot be mutated when parent run is CALCULATED/APPROVED/LOCKED/PAID | Trigger `prevent_payroll_result_mutation` | State constraint | DB level |
| RES6 | net_pay must be numeric | `net_pay NUMERIC NOT NULL` | Validation | DB level |
| RES7 | Net pay cannot be negative | No CHECK constraint; by design allows negative (overpayment recovery) | Calculation | **Not enforced** |
| RES8 | status indicates SUCCESS or FAILED | No ENUM constraint in schema | State constraint | Application level only |
| RES9 | component_trace_jsonb records per-component steps | Nullable; absent for legacy executor path | Calculation | Not enforced (nullable) |

---

## PAYROLL INPUT

| # | Rule | Source | Type | Enforced At |
|---|------|--------|------|-------------|
| INP1 | Must belong to a workspace | `payroll_input.workspace_id FK NOT NULL` | Validation | DB level |
| INP2 | Must reference a valid employee | `payroll_input.employee_id FK NOT NULL` | Validation | DB level |
| INP3 | input_code and input_category required | NOT NULL | Validation | DB level |
| INP4 | payroll_run_id=NULL means unclaimed | Nullable FK | State constraint | Application level only |
| INP5 | Input with reference_date ≤ period_end is claimed by the run | `WHERE reference_date IS NULL OR reference_date <= period_end` | Temporal rule | Application level only |
| INP6 | Input with reference_date > period_end is never claimed (future staging) | Same query exclusion | Temporal rule | Application level only |
| INP7 | Claimed inputs are not claimed again | `WHERE payroll_run_id IS NULL` filter | State constraint | Application level only |
| INP8 | Valid input_code values derived from active payroll rules | `_load_workspace_input_codes()` | Validation | Application level only |
| INP9 | Same employee can have multiple inputs with same code and reference_date | No uniqueness constraint | Validation | **Not enforced** |
| INP10 | Quantity must be positive for earnings | No CHECK constraint on sign or magnitude | Validation | **Not enforced** |

**Orphaned fields:** `payroll_input.source` — stored; never read after creation. `payroll_input.input_json` — nullable JSONB; no known reads in execution code.

---

## PAYROLL RECONCILIATION

| # | Rule | Source | Type | Enforced At |
|---|------|--------|------|-------------|
| RC1 | One reconciliation record per payroll run | `UNIQUE (payroll_run_id)` | Validation | DB level |
| RC2 | expected_total reflects payroll_run.total_net_pay at time of reconciliation | Populated by application at INSERT; not recomputed | Calculation | Application level only |
| RC3 | MATCHED if actual equals expected, MISMATCH otherwise | Comparison in `reconciliation_service.py:62` | Calculation | Application level only |
| RC4 | Reconciliation accepted at any run lifecycle state | No state gate | State constraint | **Not enforced** |
| RC5 | MISMATCH triggers no corrective action | Status stored; no workflow follows | State constraint | **Not enforced** |
| RC6 | Second reconciliation raises unhandled IntegrityError (500) | Unique constraint not caught at API layer | Validation | DB level — **unhandled** |

---

## AUDIT LOG & EVENT STORE

| # | Rule | Source | Type | Enforced At |
|---|------|--------|------|-------------|
| AL1 | Every status transition should produce an audit_log entry | `build_transition_audit()` generates payloads | Validation | Application level only |
| AL2 | Audit log records are immutable | No UPDATE/DELETE trigger; convention only | State constraint | **Not enforced** |
| AL3 | performed_by currently hardcoded to "admin@internal" | `payroll.py` | Validation | **Not enforced** |
| EV1 | Every status transition should publish an event | `build_transition_event()` payloads | State constraint | Application level only |
| EV2 | No read API for event_store or audit_log | Tables populated; no query endpoints | State constraint | Not enforced (dead output) |

---

## CROSS-TABLE RULES

| # | Rule | Source | Type | Enforced At |
|---|------|--------|------|-------------|
| X1 | Run totals must equal sum of its results | No DB constraint | Calculation | **Not enforced** |
| X2 | Payroll result cannot exist without a run | `payroll_result.payroll_run_id FK NOT NULL` | Validation | DB level |
| X3 | Employee cannot appear in a result for a run from a different workspace | No cross-workspace FK guard | Validation | **Not enforced** |
| X4 | Statutory rule used must match workspace's country | Resolved at query time; no FK on payroll_run to statutory_rule | Temporal rule | Application level only |
| X5 | Claimed inputs must belong to same workspace as run | WHERE clause in repo; no DB-level guard | Validation | Application level only |
| X6 | payroll_run.total_net_pay used for reconciliation may be stale after retry | G1 — no recalculation | Calculation | **Not enforced** |

---

## ORPHANED SCHEMA FIELDS

| Field | Table | Notes |
|---|---|---|
| `pay_cycle.definition_json` | `pay_cycle` | Stored; never read |
| `pay_cycle.run_day / cutoff_day / payment_day` | `pay_cycle` | Stored; never validated against period |
| `employee_contract.grade_id` | `employee_contract` | FK populated; never read in calculation |
| `employee_contract.designation_id` | `employee_contract` | Same |
| `grade.description` | `grade` | Never read |
| `designation.description` | `designation` | Never read |
| `salary_definition.effective_from / effective_to` | `salary_definition` | Stored; never checked |
| `payroll_input.source` | `payroll_input` | Written at creation; never read after |
| `payroll_input.input_json` | `payroll_input` | No known reads |
| `payroll_result.component_trace_jsonb` | `payroll_result` | Absent for legacy path; no structured reader |
| `payroll_run.pay_date` | `payroll_run` | Stored from UI; never used in calculation |
| `payroll_run.rule_set_id` | `payroll_run` | NULL for default runs; only set on override |
| `statutory_rule.state` | `statutory_rule` | DRAFT/LIVE states exist; not checked at rule resolution |
